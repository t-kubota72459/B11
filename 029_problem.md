<style>
/* 1. 地の文章の中にあるコード（` `で囲った部分）の色を濃くする */
code {
    color: #C0392B !important; /* 濃いレンガ色 */
    background-color: #f8f8f8;  /* 背景を少しグレーにするとさらに読みやすい */
    padding: 2px 4px;
    border-radius: 3px;
    font-weight: bold;
}

/* 2. コードブロック（枠組み）の中の文字列も改めて指定 */
code .highlight .s, 
code .highlight .s1, 
code .highlight .s2, 
.hljs-string {
    color: #C0392B !important;
    font-weight: bold;
}

/* 3. コメントの色も視認性アップ */
code .highlight .c1,
.hljs-comment {
    color: #2F4F4F !important;
}
</style>
# 工業数学：第1回・第2回 合同課題指示書

本講義の課題は、**Google Colab** で作成したノートブック（.ipynb）を **Google Classroom** へ提出します。

---

## 1. ノートブックの準備

* **ファイル名**: `工業数学_1_2_合同課題_学籍番号_氏名.ipynb`
* **構成**:  
  自分でセクション見出し（Markdown）を使って、第1回と第2回の内容を **分かりやすく** 区別すること。

## 2. 第1回セクション：Python環境の習得と基本演算

### 2.1 動作確認（コードセル）

以下のコードを実行し、自分の名前が表示されることを確認せよ。
```python
print("工業数学の学習を開始します。氏名") # 「氏名」は自分の名前に変更すること
```

### 2.2 基本演算（コードセル）

次の計算式を Python で計算し、結果を表示せよ。
* 123 + 456 * 789
* (1 + 2) * (3 + 4)
* 2の16乗 （※Pythonでは `2**16` と入力する）

### 2.3 報告（テキストセル）

* **客観的な報告の練習**:
  - 計算の結果、2の16乗がいくつになったか記述せよ。
  - **記述例**: 「計算の結果、2の16乗は 65536 であることを確認した。」
  - **注意**: 「〜だと思う」等の主観的表現は避けること。

## 3. 第2回セクション：単位・接頭語と指数表記の実践

### 3.1 物理量の変数代入と計算（コードセル）

以下の物理量を変数に代入し、電圧 V を算出せよ。

* **変数代入**:
  - 抵抗 R = 2.2 kΩ （`2.2e3` と入力）
  - 電流 I = 4.5 mA （`4.5e-3` と入力）
* **計算と表示**:
  - オームの法則 $V = I \times R$ を計算し、`print(V)` で結果を表示せよ。

### 3.2 単位変換の確認（コードセル）

* 0.000001 を指数表記（`1e-6`）を使って変数 `c` に代入し、表示せよ。
  
  ```python
  c = [________]
  print(c)
  ````

### 3.3 例題：動画のアップロード時間の計算（真似して入力してみよう）

スマホで撮影した動画（$450$ MB）を、毎秒 $15$ MB 送信できる速度でアップロードする場合にかかる時間を計算します。

**【入力例】**

以下のプログラムを新しいセルに入力して実行し、結果が $30.0$ （秒）になることを確認せよ。

```python
video_size = 450e6  # 450メガバイト
speed = 15e6        # 15メガバイト毎秒
time = video_size / speed
print(time)
```

<div style="page-break-before:always"></div>

### 3.4 変数の使い方チェック：文章題

次の問題を Google Colab(Python) で問いてみましょう。  
わたしは 750 円、お兄ちゃんとお姉ちゃんは 1000 円ずつ出して、お父さんに 2380 円のネクタイを買いました。このとき、おつりはいくらですか？（小学４年生）

⏹️ 解答の条件：
* **いきなり式を書かないで、下の変数名を使って解くこと**
  * 自分が出したお金： `jibun`
  * 兄と姉が出したお金： `kyoudai` （一人分）
  * ネクタイの値段： `tie_price`

### 3.5 単位の変換チェック：文章題

次の問題を Google Colab(Python) で問いてみましょう。  
分速 80m で歩く D さんが 36 秒間歩くと何m 進みますか。(小学６年生)

⏹️ 解答の条件：
 
* **いきなり式を書かないで、下の変数名を使って解くこと**
  * D さんの歩く速度 (分速を秒速に変換)： `speed`
  * D さんが歩いた秒数： `byou` 
  * D さんが歩いた距離： `kyori`

### 3.6 発展問題：大容量データのバックアップ (コードセル)

**上の例題を参考に**、以下の問題を解け。

PCに保存されている大量のデータを、外付けハードディスクにバックアップ（コピー）します。
完了までに **何分（ふん）** かかるか、Python の変数を使って算出せよ。

⏹️ 条件
* **バックアップするデータ量**: $1.2$ GB（$1.2 \times 10^9$ バイト）
* **書き込み速度**: $800$ kB/s（$800 \times 10^3$　バイト/秒）
* **⚠それぞれの単位に注意！！ (キロ、ギガ、秒、分)**
* **実行結果 (コードセル) を残すこと。**
 
⏹️ Python プログラムの作成

以下の空欄を自分で考えて埋め、Google Colab に入力・
```python
# 1. データの大きさを変数 size に代入
size = __________  # ← ヒント：1.2 ギガ

# 2. 書き込み速度を変数 speed に代入
speed = __________ # ← ヒント：800 キロ

# 3. かかる時間（秒）を計算して time_sec に入れる
time_sec = __________ / __________

# 4. 「分」に直して結果を表示
time_min = time_sec / 60
print(time_min)
```

### 3.7 考察（テキストセル）
* **エンジニアの視点**:  
「0.000001」という表記と「1e-6」という表記を比較し、設計ミスや数値の読み間違いを防ぐ観点から、どちらが優れていると考えるか。理由を添えて論理的に述べよ。

---

## 4. 提出方法（Google Classroom）

1. **保存**: Colabメニューの `ファイル` ＞ `保存` をクリックする。
2. **ダウンロード**: `ファイル` ＞ `ダウンロード` ＞ `.ipynb をダウンロード` を選択する。
3. **提出**: Google Classroom の課題に、ダウンロードしたファイルを添付して「提出」ボタンを押す。

### **提出期限**: 次回の講義開始時刻まで

### 3.4~3.6 どーしてもわからない人用２次元コード (QR コードは登録商標)
<div align="left">
<img src="images/fig_2026-04-17-11-27-50.png" width="10%">
</div>