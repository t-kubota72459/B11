# 工業数学 第8回：2次関数のグラフと最大・最小
―― 放物線を描き、システムの「最適解（ピーク）」を突き止めろ ――

## 1. 2次関数とは：加速する変化を司る「土管」

前回、関数とは「数値を放り込むと、ルールに従って別の数値を返す土管（変換器）」だと学びました。
今回登場する **2次関数** は、入力 $x$ が2乗（ $x^2$ ）になって効いてくる土管です。

$$y = ax^2$$

1次関数（直線）が「一定のペースで増え続ける」のに対し、2次関数は「**一気に加速して増える（または減る）**」という特徴を持っています。

### 物理空間における2次関数の例

* **物を落とした時の落下距離 ($y$) と時間 ($x$)**
  * 時間が2倍になると、落ちる距離は $2^2 = 4$ 倍に加速します（自由落下： $y = 4.9x^2$ ）。
* **車のブレーキ停止距離 ($y$) と速度 ($x$)**
  * 速度が2倍になると、止まるまでに必要な距離は $4$ 倍に跳ね上がります（ $y = kx^2$ ）。スピードの出し過ぎが危険なのは、この「2次関数の土管」が牙をむくからです。

---

## 2. 前回の復習 ＆ Matplotlibで2次関数のグラフを描く
まずは、前回みんながマスターした `matplotlib` を使って、2次関数の基本形である「放物線（ほうぶつせん）」を画面に描いてみましょう。

Google ドライブから供給されたColabファイル（または新しいセル）に以下のコードを記述して、実行してください。

```python
import matplotlib.pyplot as plt
import numpy as np

# -5 から +5 までを100分割したデータ（x軸）を作成
x = np.linspace(-5, 5, 100)

# 2次関数の土管（y = x^2）
y = x**2

# グラフの描画設定（デジタル方眼紙の準備）
fig, ax = plt.subplots(figsize=(6, 6))

# この行が図を書いているメイン
ax.plot(x, y, color="blue", linewidth=2, label="y = x^2")

# 見栄えの調整（十字の軸線と方眼目盛り）
# ここは見栄えなので、あまり気にせず。
ax.axhline(0, color="black", linewidth=0.5)
ax.axvline(0, color="black", linewidth=0.5)
ax.grid(True, linestyle="--", alpha=0.6)
ax.set_xlabel("x")
ax.set_ylabel("y")
ax.legend()
plt.show()

```

> **🎨 実験ワーク：**
> コードの `y = x**2` の前にマイナスをつけて `y = -x**2` に書き換えて実行してみましょう。グラフの向きがどう変わるかを確認してください。
> * **$a > 0$（プラス）のとき**：下に凸（お椀型 ＝ 一番底に「**最小値**」がある）
> * **$a < 0$（マイナス）のとき**：上に凸（山型 ＝ 一番てっぺんに「**最大値**」がある）
> 
> 

---

## 3. パラメーター（頂点）を動かす：標準形 $y = a(x - p)^2 + q$

1次関数で切片 $b$ を変えると直線が上下にズレたように、2次関数もパーツを付け足すことで、放物線を方眼紙の好きな場所へシフト（平行移動）させることができます。

$$y = a(x - p)^2 + q$$

この形を2次関数の **標準形** と呼び、グラフの尖端である「**頂点の座標が $(p, q)$ になる**」という超重要な性質を持っています。(教科書 pp.63)

**x がちょうど p になったとき、第１項は $0$ になります。そして $y$ は $q$ になります。**

### 💻 実験ワーク：頂点をグリグリ動かしてみよう

配布されたColabの次のセルにあるコードを実行します。

`p` と `q` の値を自由に書き換えて、山のてっぺん（またはお椀の底）がどこへ引っ越すか観察してください。`a` も変更してみましょう。

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(-5, 5, 100)

# ★ここを書き換えて実験してみよう！
a = -1   # マイナスなので「山型（上に凸）」
p = 2    # 横方向のズレ
q = 3    # 縦方向のズレ

# 2次関数の標準形
y = a * (x - p)**2 + q

# グラフ描画
fig, ax = plt.subplots(figsize=(6, 6))
ax.plot(x, y, color="red", linewidth=2, label=f"y = {a}(x - {p})^2 + {q}")

# 頂点を青丸でプロット
ax.scatter(p, q, color="blue", s=100, zorder=5, label=f"Vertex: ({p}, {q})")

ax.axhline(0, color="black", linewidth=0.5)
ax.axvline(0, color="black", linewidth=0.5)
ax.grid(True, linestyle="--", alpha=0.6)
ax.set_xlim(-5, 5)
ax.set_ylim(-5, 5)
ax.legend()
plt.show()

```

---

## 4. 【練習ワーク】SymPyを使って教科書の基本問題を解いてみよう

ここまで、2次関数のグラフが頂点 $(p, q)$ によって配置が決まることを見てきました。

では、教科書によくある「条件から2次関数の式を逆算する問題」を、**SymPy** を使ってコンピュータに解かせてみましょう。

みんながノートに書く「手計算のステップ」を、そのままプログラムに翻訳していきます。

### 📝 練習問題 教科書 pp.66 問 7.6 (1)

> **【問題】**
> 頂点が $(3, -1)$ で、点 $(1, 3)$ を通る2次関数の式を求めよ。

## 5. 【設計・モダン実務演習】教科書の問題に SymPyを使って解く！

エンジニアの設計現場では、複雑な数式を手計算していると計算ミス（ヒューマンエラー）に繋がります。そこで現代のエンジニアは、ツールを使ってコンピュータに数式を処理させ、そのままグラフへ連動させて最適解（最大・最小）をビジュアルで突き止めます。

さきほど学んだSymPyには、手計算の面倒なプロセスを一瞬で代行してくれる自動平方完成コマンド（`sym.completing_square`）が用意されています。これを使って教科書の実践問題に挑戦しましょう。

---

### 📝 実践演習：トタン板を曲げて作る雨樋（あまどい）の断面積最大化

> **【問題】**
> 幅 $20\text{cm}$ の長方形のトタン板を、両端から同じ長さ $x\text{cm}$ ずつ直角に折り曲げて、断面積が最大の雨樋を作りたい。
> 1. 断面積 $y\text{cm}^2$ を表す2次関数を求めよ。
> 2. 断面積 $y$ が最大になるときの折り曲げ幅 $x$ と、そのときの最大面積を求めよ。
> 


```
                      
        ─┐           ┌─
        x│     y     │x
         └───────────┘ 
         ◀── 20 - 2x ──▶

```

#### 💻 SymPy ＆ Matplotlib 連携コード（自動平方完成版）

以下のコードをColabに貼り付けて実行してください。
組み立てた数式を、プログラムが自動で平方完成し、グラフのどこが「てっぺん（最大値）」かを教えてくれます。


---

### 📋 第8回 レポート提出課題：加工断面積の最大化（相似比の自動設計）

> **【課題】**
> 下の図のように、底辺 $12\text{cm}$、高さ $8\text{cm}$ の直角三角形の材料から、内側にぴったり収まるような長方形のブロックを削り出したい。
> 1. 前半の授業で解説した通り、長方形の縦の長さを $x\text{cm}$ とすると、面積の式は $y = x \cdot (12 - 1.5x)$ となる。
> 2. この式を、上の雨樋の例にならって **SymPyの `sym.completing_square` コマンドを使って自動で平方完成させるPythonコード** を記述せよ。
> 3. 計算によって出力された「平方完成された式」と、そこから読み取れる「最大面積になる縦の長さ $x$ 」をレポート用紙に記述して提出すること。
> 
> 

```
       ▲
      ╱┃
     ╱ ┃ 8cm
    ╱  ┃
   ╱🔲🔲┃
  ╱ 🔲🔲┃ ▲
 ╱  🔲🔲┃ ┃ x
 ━━━━━━━┛ ▼
 ◀ 12cm ▶

```

---

## 6. 工業数学としての結論

なぜコンピュータのプログラムや自動制御で2次関数が飛び交うのか。
それは、機械の制御やエネルギーのロス、強度設計の多くが「**2乗に比例する**」という物理法則を持っているからです。

手計算で平方完成をして頂点を求めるのも大切ですが、現代のエンジニアリングにおいて最も強力なのは、今回みんなが体験した「SymPyのようなツールに数式を丸投げし、プログラムと連携させて瞬時に最適解（ピーク ＝ 最大・最小）を視覚化する能力」です。

1次関数の「直線」から始まったこのシリーズ。2次関数の「カーブ（放物線）」をPythonで完全にコントロールできるようになった君たちは、システムの設計において「一番効率が良いポイント」を自動で叩き出す、本物のエンジニアの入り口に立ちました。

```

```