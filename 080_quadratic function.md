# 工業数学 第8回：2次関数のグラフと最大・最小
―― 放物線を描き、システムの「最適解（ピーク）」を突き止めろ ――

## 1. 2次関数とは：加速する変化を司る「土管」

前回、関数とは「数値を放り込むと、ルールに従って別の数値を返す土管（変換器）」だと学びました。
今回登場する **2次関数** は、入力 $x$ が2乗（ $x^2$ ）になって効いてくる土管です。

$$y = ax^2$$

1次関数（直線）が「一定のペースで増え続ける」のに対し、2次関数は「**一気に加速して増える（または減る）**」という特徴を持っています。

### 物理空間における2次関数の例

* **物を落とした時の落下距離 ($y$) と時間 ($x$)**
  * 時間が2倍になると、落ちる距離は $2^2 = 4$ 倍に加速します（自由落下： $y = 4.9x^2$ ）。
* **車のブレーキ停止距離 ($y$) と速度 ($x$)**
  * 速度が2倍になると、止まるまでに必要な距離は $4$ 倍に跳ね上がります（ $y = kx^2$ ）。スピードの出し過ぎが危険なのは、この「2次関数の土管」が牙をむくからです。

---

## 2. 前回の復習 ＆ Matplotlibで2次関数のグラフを描く

まずは、前回みんながマスターした `matplotlib` を使って、2次関数の基本形である「放物線（ほうぶつせん）」を画面に描いてみましょう。

Colabファイル を新規作成し、以下のコードを記述して、実行してください。

```python
import matplotlib.pyplot as plt
import numpy as np

# -5 から +5 までを100分割したデータ（x軸）を作成
x = np.linspace(-5, 5, 100)

# 2次関数の土管（y = a*x^2）
a = 1
y = a*x**2

# グラフの描画設定（デジタル方眼紙の準備）
fig, ax = plt.subplots(figsize=(6, 6))

# この行が図を書いているメイン
ax.plot(x, y, color="blue", linewidth=2, label="y = x^2")

# 見栄えの調整（十字の軸線と方眼目盛り）
# ここは見栄えなので、あまり気にせず。
ax.axhline(0, color="black", linewidth=0.5)
ax.axvline(0, color="black", linewidth=0.5)
ax.grid(True, linestyle="--", alpha=0.6)
ax.set_xlabel("x")
ax.set_ylabel("y")
ax.legend()
plt.show()
```

> **🎨 実験ワーク：**
> コードの `y = x**2` の前にマイナスをつけて `y = -x**2` に書き換えて実行してみましょう。グラフの向きがどう変わるかを確認してください。
> * **$a > 0$（プラス）のとき**：下に凸（お椀型 ＝ 一番底に「**最小値**」がある）
> * **$a < 0$（マイナス）のとき**：上に凸（山型 ＝ 一番てっぺんに「**最大値**」がある）

---

## 3. パラメーター（頂点）を動かす：標準形 $y = a(x - p)^2 + q$

1次関数で切片 $b$ を変えると直線が上下にズレたように、2次関数もパーツを付け足すことで、放物線を方眼紙の好きな場所へシフト（平行移動）させることができます。

$$y = a(x - p)^2 + q$$

2 次関数を上のような式に変形すると、グラフの尖端である「**頂点の座標が $(p, q)$ になる**」という超重要な性質を持っています。
このような式の形を標準形といい、標準形に直すことを平方完成と言います。(教科書 pp.63)

> **x がちょうど p のとき、第１項は $0$ になります。そして $y$ は $q$ になります。**

### 💻 実験ワーク：頂点をグリグリ動かしてみよう

次のセルに以下のコードをコピペし実行します。

`p` と `q` の値を自由に書き換えて、山のてっぺん（またはお椀の底）がどこへ引っ越すか観察してください。`a` も変更してみましょう。

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(-5, 5, 100)

# ★ここを書き換えて実験してみよう！
a = -1   # マイナスなので「山型（上に凸）」
p = 2    # 横方向のズレ
q = 3    # 縦方向のズレ

# 2次関数の標準形
y = a * (x - p)**2 + q

# グラフ描画
fig, ax = plt.subplots(figsize=(6, 6))
ax.plot(x, y, color="red", linewidth=2, label=f"y = {a}(x - {p})^2 + {q}")

# 頂点を青丸でプロット
ax.scatter(p, q, color="blue", s=100, zorder=5, label=f"Vertex: ({p}, {q})")

ax.axhline(0, color="black", linewidth=0.5)
ax.axvline(0, color="black", linewidth=0.5)
ax.grid(True, linestyle="--", alpha=0.6)
ax.set_xlim(-5, 5)
ax.set_ylim(-5, 5)
ax.legend()
plt.show()
```

---
## 4. 平方完成の練習

平方完成するのも Python を使えば簡単にできます。

```python
import sympy as sp

a, x, p, q = sp.symbols( 'a x p q' )

# 元の式
f1 = x**2 - 4*x + 7

# 平方完成の式
f2 = a*(x - p)**2 + q

shiki = sp.Eq(f1, f2)
print(sp.solve(shiki, [a, p, q]))
```

## 4.1 練習　教科書 pp.65 問 7.4

次の２次関数を標準形にしてグラフを描け。(グラフはこのままの式でもコンピュータを使えば描けちゃうけど)

(1) $y = x^2 - 4x + 1$  
(2) $y = -x^2 - 2x$  
(3) $y = \frac{1}{2}x^2-2x+3$  
(4) $y = 2^x -2x-3$

答は pp.227

## 5. 【練習ワーク】SymPyを使って教科書の基本問題を解いてみよう

ここまで、2次関数のグラフが２次関数の標準形の頂点 $(p, q)$ によって配置が決まることを見てきました。

では、教科書によくある「条件から2次関数の式を逆算する問題」をコンピューターに解かせてみましょう。

エンジニアの設計現場では、複雑な数式を手計算していると計算ミス（ヒューマンエラー）に繋がります。そこで現代のエンジニアは、ツールを使ってコンピュータに数式を処理させ、そのままグラフへ連動させて最適解（最大・最小）をビジュアルで突き止めます。

### 📝 実践演習：トタン板を曲げて作る雨樋（あまどい）の断面積最大化

> **【問題】**
> 幅 $20\text{cm}$ の長方形のトタン板を、両端から同じ長さ $x\text{cm}$ ずつ直角に折り曲げて、断面積が最大の雨樋を作りたい。
> 1. 断面積 $y\text{cm}^2$ を表す2次関数を求めよ。
> 2. 断面積 $y$ が最大になるときの折り曲げ幅 $x$ と、そのときの最大面積を求めよ。


```
        ─┐           ┌─
        x│     y     │x
         └───────────┘ 
```

トタン板を折り曲げて雨樋を作るこの問題は、長方形の「縦と横の長さ」を $x$ を使って表し、面積 $y$ の式（2次関数）を立ててから **平方完成** することで、最大値とそのときの $x$ の値をスマートに求めることができます。

## 1. 断面積 $y$ を表す2次関数を求める

雨樋の断面は、底面と両脇の壁でできた「長方形（上面が開いた形）」になります。

* **両端の折り曲げた部分（縦の長さ）**： $x\text{ cm}$
* **底面になる部分（横の長さ）**： 全体の幅 $20\text{ cm}$ から、両端の $x\text{ cm}$ を2つ分引くので、$(20 - 2x)\text{ cm}$ となります。

長方形の面積 $y$ は `縦 × 横` で表せるので、式は以下のようになります。

$$y = x(20 - 2x)$$

$$y = -2x^2 + 20x$$

> 💡 **注意（変数の範囲）**
> 長さや幅は必ずプラスでなければならず、底面の長さ $(20 - 2x)$ もプラスである必要があるため、 $x$ のとり得る範囲（定義域）は **$0 < x < 10$** になります。

## 2. 平方完成して最大値を求める

先ほど求めた 2次関数 $y = -2x^2 + 20x$ を平方完成してみます。人間がやると以下の通りです。

1. $x^2$ の係数である `-2` で括ります。

$$y = -2(x^2 - 10x)$$

2. 括弧の中を $(x - \text{半分})^2$ の形にします（$10$ の半分は $5$ ）。

$$y = -2\{(x - 5)^2 - 25\}$$

3. 括弧を展開して整理します。

$$y = -2(x - 5)^2 + 50$$

が、コンピュータにやらせると以下のプログラムになります。

```python
import sympy as sp
a, x, p, q = sp.symbols( 'a x p q' )

f1 = -2*x**2 + 20*x
f2 = a*(x - p)**2 + q

print( sp.solve(sp.Eq(f1, f2), [a, p, q]))
```

この式から、グラフは「上に凸の放物線」で、頂点が $(5, 50)$ であることがわかります。
$x = 5$ は定義域（$0 < x < 10$）の中にぴったり収まっています。

### 【解答】

* **1の答え：** $y = -2x^2 + 20x \quad (0 < x < 10)$
* **2の答え：** 折り曲げ幅 $x = 5\text{ cm}$ のとき、最大面積は $50\text{ cm}^2$

---

## 6. 工業数学としての結論

なぜコンピュータのプログラムや自動制御で2次関数が飛び交うのか。
それは、機械の制御やエネルギーのロス、強度設計の多くが「**2乗に比例する**」という物理法則を持っているからです。

手計算で平方完成をして頂点を求めるのも大切ですが、現代のエンジニアリングにおいて最も強力なのは、今回みんなが体験した「SymPyのようなツールに数式を丸投げし、プログラムと連携させて瞬時に最適解（ピーク ＝ 最大・最小）を視覚化する能力」です。

1次関数の「直線」から始まったこのシリーズ。2次関数の「カーブ（放物線）」をPythonで完全にコントロールできるようになった君たちは、システムの設計において「一番効率が良いポイント」を自動で叩き出す、本物のエンジニアの入り口に立ちました。
