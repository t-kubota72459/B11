# 📝 【第9回・中間レポート】総合設計演習：AGV・EVの制御と限界航続距離の設計

- 本講義の課題は、**Google Colab** で作成したノートブック（.ipynb）を **Google Classroom** へ提出します。

- 以下の内容を1つのノートブックにまとめ、期限までに提出してください。
- テキストセルとコードセルを使って、わかりやすくレイアウトしてください。
- **提出先**: Google Classroom 課題「第9回」にファイルを添付して提出。

  1) [ファイル] > [ダウロード] > [.ipynb をダウンロード]
  2) ダウンロードフォルダにある `工業数学_9_学籍番号_氏名.ipynb` を提出する。

## **【ミッション】**

あなたはスマート工場に導入される自動搬送ロボット（AGV）、および省エネEVカーの設計・制御担当エンジニアです。これまでの授業で学んだPython（SymPy, Matplotlib）と数学的知識を総動員し、以下の5つの設計課題を解決してください。

各課題のコードの `【穴埋め】` 部分を書き換えて実行し、導き出された「論理的な設計根拠（数値と理由）」をレポートに記述して提出すること。

---

### 📋 課題1：【制動距離の逆算】（代数計算）

> **【状況】**
> 機体が速度 $v\text{ (m/s)}$ で走行しているとき、ブレーキをかけてから完全に停止するまでの制動距離 $D\text{ (m)}$ は、以下の2次方程式でモデル化される。
> 
> $$D = 0.5v^2 + 1.2v$$
> 
> **【ミッション】**
> 前方 $10\text{m}$ の位置に障害物を検知した。衝突せずに安全に停止するためには、走行速度 $v$ を何 $\text{m/s}$ 以下に制限しておく必要があるか。
> 1. $D = 10$ となるような等式（方程式）を立てよ。
> 2. 以下のコードを完成させて実行し、物理的に正しい（正の数の）速度 $v$ を特定せよ。
> 
 

```python
import sympy as sp

v = sp.symbols('v')

# ==========================================================
# 【穴埋め】停止距離が 10m になるときの等式を立ててください。
# ==========================================================
shiki_1 = sp.Eq(【ここに左辺の式を入力】, 【ここに右辺の数値を入力】)
ans_1 = sp.solve(shiki_1, v)

print(f"安全に停止するための限界速度 v: {ans_1} [m/s]")

```

---

### 📋 課題2：【センサー信号のマスク処理】（論理とビット）

> **【状況】**  
> 機体の周囲には8個の障害物検知センサーがあり、その反応は1つの8ビットデータ（2進数）として送られてくる。（1が検知、0が安全）
> 現在のステータスは `0b11010110` である。
>
> **【ミッション】**
> 「前進」するためには、前方にある2つのセンサー（右から3番目と4番目のビット）が両方とも安全（0）でなければならない。
> 1. 前方の2つのセンサーだけを抽出するための「マスクデータ（2進数）」を作成せよ。
> 2. 以下のコードを完成させて実行し、前進可能かどうかを判定せよ。

```python
status = 0b11010110

# ==========================================================
# 【穴埋め】右から3番目と4番目だけを「1」にしたマスクを作成
# ==========================================================
mask = 0b【ここに0と1の並びを入力】

result = status & mask
print(f"抽出されたデータ: {bin(result)}")
if result == 0:
    print("前方クリア。前進可能です。")
else:
    print("前方に障害物あり。前進できません。")

```

---

### 📋 課題3：【目的地までの直線距離】（図形と三平方の定理）

> **【状況】**  
> 工場のフロアは $xy$ 座標（単位：メートル）で管理されている。AGVは現在、待機所 $(3, 5)$ にいる。次の命令が入り、部品棚の座標 $(15, 10)$ まで移動することになった。
> 
> **【ミッション】**
> 三平方の定理を用いて、現在の位置から目的地までの最短の直線距離 $L\text{ (m)}$ を計算せよ。
> 1. $x$ 座標の差分と $y$ 座標の差分を求め、ピタゴラスの定理を用いた等式（ $L^2 = \dots$ ）を立てよ。
> 2. 以下のコードを完成させて距離 $L$ を求めよ。
> 

```python
import sympy as sp
L = sp.symbols('L')

# ==========================================================
# 【穴埋め】2点 (3,5) と (15,10) の間の距離を求める等式を立てる
# ==========================================================
shiki_3 = sp.Eq(L**2, 【ここに斜辺の2乗を求める計算式を入力】)
ans_3 = sp.solve(shiki_3, L)

print(f"目的地までの直線距離 L: {ans_3} [m]")

```

---

### 📋 課題4：【モーター効率の最大化】（2次関数の最適化）

> **【状況】**  
> 搭載されている走行モーターは、流す電流 $I\text{ [A]}$ によって「推力効率 $y\text{ [\%]}$」が変化する。実験の結果、効率 $y$ は以下の2次関数になることがわかった。
> 
> $$y = -2I^2 + 16I + 40$$
> 
> **【ミッション】**
> バッテリーの電力を最も効率よく推進力に変換するための、最適な電流値 $I$ を設計したい。
> 1. 以下のコードを完成させて実行し、SymPyに平方完成を行わせよ。
> 2. 出力された計算結果から、**「最も効率が良くなる電流値 $I\text{ [A]}$ 」**と**「そのときの最大効率 $y\text{ [\%]}$」**を読み取ってレポートに記述せよ。
> 
> 

```python
import sympy as sp

a, I, p, q = sp.symbols('a I p q')

# ==========================================================
# 【穴埋め】上記の推力効率の「元の式」を入力してください
# ==========================================================
f1 = 【ここに元の式を入力】

f2 = a * (I - p)**2 + q
shiki_4 = sp.Eq(f1, f2)
ans_4 = sp.solve(shiki_4, [a, p, q])

ans_a, ans_p, ans_q = ans_4[a], ans_4[p], ans_4[q]
print(f"平方完成の結果: y = {ans_a} * (I - {ans_p})**2 + {ans_q}")

```

---

### 📋 課題5：【総合問題】限界航続距離の算出（エネワン・チャレンジ）🔥

> **【状況】**  
> 課題4で求めた「最も効率が良い電流値 $I\text{ [A]}$」のまま、平坦なコースを定速走行し続けるレース戦略を立てる。  
> 機体のシステム電圧は常に $24\text{V}$ であり、搭載されているバッテリーの総エネルギー容量は **$360\text{ Wh}$** （ワットアワー）である。  
> また、この最適電流値で走行しているときの機体の巡航速度は **$15\text{ km/h}$** であることが事前テストでわかっている。  
>
> **【ミッション】**
> バッテリーを完全に使い切るまでに、この機体は何 $\text{km}$ 走ることができるか？
> 以下の順序で物理法則に従って立式し、航続距離 $D\text{ (km)}$ を導き出せ。
> 1. **消費電力の計算**：電力 $P\text{ (W)}$ ＝ 電圧 $(\text{V})$ $\times$ 電流 $(\text{A})$
> 2. **走行可能時間の計算**：時間 $T\text{ (h)}$ ＝ バッテリー容量 $(\text{Wh})$ $\div$ 消費電力 $(\text{W})$
> 3. **航続距離の計算**：距離 $D\text{ (km)}$ ＝ 速度 $(\text{km/h})$ $\times$ 時間 $(\text{h})$
> 
> 以下のコードを完成させて、限界航続距離を求めよ。

```python
# ==========================================================
# 【穴埋め】課題4で求めた「最適な電流値 I」を入力してください
# ==========================================================
best_I = 【ここに電流値を数値で入力】

# 1. 消費電力 P [W] の計算 (電圧 24V × 電流 I)
P = 24 * best_I

# 2. 走行可能時間 T [h] の計算 (容量 360Wh ÷ 消費電力 P)
T = 360 / P

# 3. 限界航続距離 D [km] の計算 (速度 15km/h × 時間 T)
D = 15 * T

print("【レース戦略シミュレーション結果】")
print(f"モーター消費電力: {P} [W]")
print(f"走行可能時間: {T} [時間]")
print(f"限界航続距離: {D} [km]")
```
