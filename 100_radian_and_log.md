<style>
pre {
  margin-top: 0.3em !important;
  margin-bottom: 0.3em !important;
  padding: 6px 12px !important;
}
/* 1. 地の文章の中にあるコード（` `で囲った部分）の色を濃くする */
code {
    color: #b97214 !important; /* 濃いレンガ色 */
    background-color: #f8f8f8e0;  /* 背景を少しグレーにするとさらに読みやすい */
    padding: 2px 4px;
    border-radius: 3px;
    font-weight: bold;
}

/* 2. コードブロック（枠組み）の中の文字列も改めて指定 */
code .highlight .s, 
code .highlight .s1, 
code .highlight .s2, 

/*
.hljs-string {
    color: #b97214 !important;
    font-weight: bold;
}
*/
/* 3. コメントの色も視認性アップ */
code .highlight .c1,
.hljs-comment {
    color: #2F4F4F !important;
}
</style>
# 第10回 弧度法と対数：Python で「エンジニアの視覚」を手に入れる
### ー 難しい数式をシンプルに、巨大な数字を扱いやすく ー

## 今日のゴール
* **ラジアン（弧度法）** の本当の意味を理解し、なぜ人間都合の「度数法（°）」を卒業するのかを納得する。
* 半径が大きくなっても「角度と弧の長さが完全に比例する」ダイナミズムを、Python の重ね書きグラフで体感する。
* **対数（log）** が「爆発的に巨大な（または極小の）数字」を扱いやすいサイズへ圧縮する魔法であることを知る。
* **対数グラフ（ログスケール）** の威力を Colab で描き、工学の現場でデータがどう整理されるかを学ぶ。

---

今日の講義は、皆さんがこれまで「ただ暗記させられて苦痛だった」数学の2大巨頭、**【ラジアン】** と **【対数】** を扱います。数式をゴリゴリ解くのではなく、Python の力を借りて「目で見て納得する」スタイルで進めます！

---

## 2. 【前半】なぜ度数法（°）を捨てて「ラジアン」を使うのか？

高校まで、私たちは「1周＝360度」という **度数法** を使ってきました。しかし、理工学やプログラミングの世界（SymPyやMatplotlibなど）では、角度に `90` や `180` を入れると思った通りのグラフが描けません。なぜなら、彼らは全員 **ラジアン（弧度法）** という世界標準規格で動いているからです。

> **💡 なぜ度数法を卒業するの？** > 「360度」という数字は、大昔の人間が「1年がだいたい365日だから、きりの良い360にしよう」と決めた、完全に **人間の都合（人為的な数字）** です。  
> これを使っていると、エンジニアが最もよく使う「弧の長さ $l$ を求める公式」が次のように汚くなってしまいます。
> $$l = 2\pi r \times \frac{\theta}{360}$$
> 数式に $\frac{2\pi}{360}$ という「人間の都合のゴミ」がこびりついてしまい、計算ミスや数式の複雑化を招くのです。

### ① ラジアンの定義：円そのものの性質から角度を決める
ゴミを消し去るために、エンジニアたちは **「半径 $r=1$ の円（単位円）において、弧の長さがピッタリ $1$ になるときの中心角を『 $1$ ラジアン（$1\text{ rad}$）』と呼ぼう！」** と決めました。

このように、円の半径と弧の長さという「自然のカタチ」から直接決めた単位が **弧度法** です。

### ② 【Colabで実験】半径が変わったらどうなる？（実行例セクション1）

配布された Colab ノートブックの **「1. ラジアン (弧度法) とは」** のコードブロックを実行してみましょう。

実行すると、以下のような **「半径1と半径2の扇形が綺麗に重なったグラフ」** が左下にメッセージ付きで表示されます。

* **赤い円弧：** 半径 $r = 1$ のとき、中心角が $1\text{ rad}$ なら、弧の長さはピッタリ **$l = 1$**
* **青い円弧：** 半径 $r = 2$ のとき、中心角が $1\text{ rad}$ なら、弧の長さはピッタリ **$l = 2$**

> **👉 ここがポイント！** > 中心角（$1\text{ rad}$）が固定されているとき、
> * 半径が 1 倍（$r=1$）なら ➡ 弧の長さも 1 （$l=1$）
> * 半径が 2 倍（$r=2$）なら ➡ 弧の長さも 2 （$l=2$）
> 
> つまり、**「半径と弧の長さが完全にきれいに比例（相似）する」** のです。  
> だから、弧の長さを求める公式は、あのややこしい分数なしで、次の世界一シンプルな掛け算だけで終わります。
> $$\text{弧の長さ} = \text{半径} \times \text{角度} \quad (l = r\theta)$$

度数法のゴミが完全に消え去りましたね！これが、理工学の計算でラジアンが圧倒的に愛されている理由です。

### ウォーミングアップ

いままでの数学あるある問題 (教科書 pp.123)。
* $45^\circ$ をラジアンに直せ
* $120^\circ$ をラジアンに直せ

```python
import sympy as sp

# 半円 (180度) はπ、そして一周 (360度) は 2π
en = 2 * sp.pi
rad =【数式】* en
display(rad)
```

### 【問題】

> あなたは工場の自動搬送ロボット（AGV）の制御プログラムを開発しています。  
> ロボットの車輪の半径は $r = 15 \text{ cm}$ です。車輪に取り付けられたセンサーから「車輪がピッタリ $\theta = 4.5 \text{ rad}$ 回転した」というデータが送られてきました。  
> 1. このとき、ロボットが前進した距離 $l \text{ [cm]}$ を求める世界一シンプルな数式を立て、Python（SymPy）で計算しなさい。
> 2. もし、このロボットのセンサーがラジアンではなく、昔ながらの度数法（度）で $257.8^\circ$ とデータを送ってきたとしたら、前進した距離を求める計算式はどうなるか書きなさい。どちらの単位がプログラムを組む上で楽（バグが出にくい）か、理由とともに答えなさい。

**SymPy コード**

```python
import sympy as sp

# 変数を定義
r, theta = sp.symbols('r theta')

# 弧の長さ（走行距離）の公式を定義
l = 【数式】

# 半径15cm、角度4.5rad を代入して計算
ans = l.subs({r: 【数値】, theta: 【数値】}) # 

print("AGVの進んだ距離は：")
display(ans)
```
---

## 3. 【後半】巨大な数字を圧縮する魔法「対数（log）」（目安：40分）

理工系の現場（電気、音響、地震、化学など）では、**「桁違いに巨大な数字」**や **「気が遠くなるほど極小の数字」** が日常茶飯事です。

例えば、人間の耳が聴き取れる「かすかな音のエネルギー」を $1$ とすると、飛行機のジェットエンジンの音は **$1,000,000,000,000$（1兆）** に達します。これをそのまま普通の目盛りでグラフに描こうとするとどうなるでしょうか？

### ① 対数（log）の正体は「0 の個数（桁数）」
そんな「桁違いの怪物」たちを、人間の目で扱いやすいサイズにギュッと圧縮してくれる魔法が **対数（$\log$）** です。

難しく考える必要はありません。底（ベース）を 10 としたときの対数 $\log_{10} x$（常用対数）の正体は、ひとことで言えば **「 $10$ の後ろにある $0$ の個数（桁数）」** です。

* $\log_{10} 10 = 1$ （0が1個）
* $\log_{10} 100 = 2$ （0が2個）
* $\log_{10} 1,000,000,000,000 = 12$ （0が12個＝12桁）

あの恐ろしい「1兆」という大爆発したデータが、対数のフィルターを通すだけで、たったの `12` という引き締まった数字に早変わりします。この仕組みを使ったのが、音の大きさを表す単位 **「デシベル（dB）」** や、酸性度を表す **「pH（ペーハー）」**、地震の規模 **「マグニチュード」** です。

### ② 【Colabで実験】普通の目盛り（線形）vs 対数目盛り（ログスケール）

配布された Colab ノートブックの **「2. 対数の必要性と対数グラフ」** のコードを実行してみましょう。
ここでは、1から10,000まで爆発的に増えていくデータを用意し、 **「普通のグラフ」** と **「対数グラフ」** を左右に並べて同時に描画させています。

グラフを見比べてみましょう。

* **左側：普通の目盛り（線形スケール）**  
  値が10,000に向かって一気に急上昇するため、前半の「1」や「10」や「100」といった小さな変化が **完全に下の軸に潰れてしまい、全く見えなくなっている** はずです。これでは実験データの小さな変化を見落としてしまいます。
* **右側：対数目盛り（ログスケール）**  
  縦軸の目盛りが `10^0(1), 10^1(10), 10^2(100), 10^3(1000), 10^4(10000)` と、 **「1目盛りごとに10倍（1桁アップ）」** していく特殊な構造になっています。これによって、小さな「1〜100」の変化も、巨大な「10,000」の変化も、すべてが均等に1枚のグラフの中に綺麗に収まって見えます。

> **💡 なぜこれをするの？**
> エンジニアは、データが広範囲に散らばっているとき、意図的にグラフの軸を「対数目盛り（ログスケール）」に切り替えます。  
> 数式を解くだけが数学ではありません。Python を使って「見えないデータを見えるように変形する」ことこそが、現場で役立つエンジニアの数学の第一歩なのです。

### ウォーミングアップ

いままでの数学あるある問題 (教科書 pp.123)。

* $\log_{10} 1000$ を求めよ。← 10 を何乗したら 1000 か？
* $\log_{2} 1024$ を求めよ。← 2 を何乗したら 1024 か？

```python
import sympy as sp

display(sp.log(【数値】, 10))
display(sp.log(【数値】, 2))
```

### 【問題】

あなたは工場の生産技術エンジニアです。新しく導入した「溶接ロボット」の騒音を測定したところ、音が持つ空気の振動エネルギー $I$ が、人間の耳に聞こえる限界の音（基準値 $I_0 = 1$）に対して $1,000,000$ 倍（100万倍）であることがわかりました。  
人間の耳に合わせた騒音の単位「デシベル $\text{[dB]}$」は、対数を使って次の公式で計算されます。

$$\text{騒音レベル } D = 10 \times \log_{10}(I)$$

1. この溶接ロボットの騒音レベル $D \text{ [dB]}$ を SymPy を使って求めなさい。
2. 隣に同じロボットをもう1台増やしたところ、音のエネルギー $I$ は「2倍（200万倍）」になりました。このときの騒音レベルは何 $\text{dB}$ になるか、SymPy で計算しなさい。（ヒント：エネルギーが2倍になっても、デシベルは2倍にはなりません。対数の魔法を確かめてみてください）

```python
import sympy as sp

# 変数を定義
I = sp.symbols('I')

# デシベルの公式を定義（底を10とする常用対数 sp.log(真数, 底)）
D = 【ここに数式】

# 1. ロボット1台（エネルギー100万倍）
ans1 = D.subs({I: 【数値】})
print("ロボット1台の騒音レベル(dB)：")
display(ans1)           # 結果: 60 dB

# 2. ロボット2台（エネルギー200万倍）
ans2 = D.subs({I: 【数値】})
print("ロボット2台の騒音レベル(dB)：")
display(ans2.evalf(4))  # 結果: 63.01 dB （わずか3dBしか増えない！）
```

---


## 4. 本日のまとめ ＆ ミニ課題（目安：15分）

### 📝 今日のまとめ
1. **ラジアン**：円の半径と弧の長さの比例関係をそのまま使うため、公式が $l = r\theta$ と最高にシンプルになる！
2. **対数**：大爆発する巨大なデータを「桁数（0の個数）」に変換し、人間の目で扱えるサイズに圧縮する！
3. **対数グラフ**：潰れて見えなかった小さな変化と、巨大な変化を同時に1つの画面で観察するための必須テクニック！

### 🛠 ミニ課題（習うより慣れろ！）
配布した Colab ノートブックの２番目にあるコードを少し弄ってみましょう。

右側の対数グラフ（ログスケール）を設定しているのは、Matplotlib の `ax2.set_yscale('log')` という、たった1行の命令です。

1. この部分をあえてコメントアウト（先頭に `#` を付ける）するか、普通の `linear` に書き換えて実行してみましょう。
2. グラフがどのように変化したか（潰れてしまったか）を自分の目で確認してください。
   