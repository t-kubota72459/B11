# 第１１回🎓 Pythonで体感する「数式」と「現実」：サイン波から紐解く電気・振動の世界

本講義資料は、数学の教科書に登場する「サイン波」という抽象的な概念が、いかにして「コンセントの電気」や「工場のマシントラブル」といった現実世界の現象と結びついているかを、Pythonによるデータ可視化を通じて体感するためのものです。

---

## 🎯 今日のゴール

今日の授業では、次のことを目標にします。

1. **$\sin(\theta)$ や $\cos(\theta)$** が、ただの図形の比率ではなく「回転」を表現する道具であることを理解する。
2. 数学の公式（**$\sin^2\theta + \cos^2\theta = 1$**）が、実は単なる「三平方の定理」であることを Python で確かめる。
3. 波の数式 **$A \sin(2\pi f t)$** の「振幅 $A$」と「周波数 $f$」が波の形をどう変えるかを説明できる。
4. 身近な**コンセントの電気**（AC 100V, 50Hz/60Hz）の正体を、Python で波として描画できる。
5. 日本の中で **50Hz と 60Hz が混在している歴史的な理由**を知る。
6. 位相や周波数がズレた波同士を合成したときに起きる現象（**打ち消し合い**や**うなり**）をシミュレーションできる。
7. 【時間があれば】電気の世界の超重要概念 **「力率（りきりつ）」** と波のズレの関係をイメージできる。
8. **逆関数（アーク関数）** が、「結果から原因（角度）を逆算する」便利な道具であることを知る。

---

## 1. 回転を表現する「道具」としての三角関数

### 1.1 $\sin(\theta)$ と $\cos(\theta)$ の正体
高校数学で習う $\sin(\theta)$ や $\cos(\theta)$ は、単なる三角形の比率の暗記ではありません。本質的には、**「ぐるぐる回る回転運動」を数式として切り出すための道具**です。

* **$\sin(\theta)$：** ぐるぐる回る点の「$y$ 軸（縦方向）に映った影」
* **$\cos(\theta)$：** ぐるぐる回る点の「$x$ 軸（横方向）に映った影」

点が円の上を 1 周するとき、その影は `0 → 1 → 0 → -1 → 0` という一定の周期を持って上下（または左右）に揺れ動きます。これが「波（サイン波）」の正体です。

### 1.2 関数とは「土管」である
プログラミングや数学に登場する「関数（Function）」は、中に何が入っているか分からない **「土管（ブラックボックス）」** だとイメージしてください。

* $\sin$ という土管の入り口に **「角度 $\theta$」** を放り込む
* 土管の出口から **「影の長さ（値）」** がポンと出てくる

> 角度 θ  ──> 【  sin(θ) の土管  】 ──>  影の長さ（値）

このシンプルなイメージを頭に植え付けておいてください。

---

## 2. 公式の呪縛を解く：$\sin^2(\theta) + \cos^2(\theta) = 1$

数学の教科書で必ず強制暗記させられる、以下の有名な公式があります。
$$\sin^2(\theta) + \cos^2(\theta) = 1$$

一見すると複雑な数式に見えますが、なんのことはありません。これは、先週やった **「三平方の定理（ピタゴラスの定理：$a^2 + b^2 = c^2$）」** そのものです。
半径（斜辺の長さ）が 「1」 の直角三角形を思い浮かべれば、縦の影の2乗（$\sin^2$）と横の影の2乗（$\cos^2$）を足したら、絶対に斜辺の2乗である「1」のまま変わらないのは当たり前です。

これを、Pythonの重ね書きプロットを使って目で確認してみましょう。

### 💻 Colabデモコード1：三平方の定理の視覚化

```python
import numpy as np
import matplotlib.pyplot as plt

# 横軸：角度 theta (0 から 2π まで)
theta = np.linspace(0, 2 * np.pi, 200)

y_sin2 = np.sin(theta)**2
y_cos2 = np.cos(theta)**2
y_sum  = y_sin2 + y_cos2  # 足し算

# 3本の線を1つのグラフに重ね書き
plt.figure(figsize=(8, 4))
plt.plot(theta, y_sin2, color='orange', linestyle=':', label='sin^2(theta)')
plt.plot(theta, y_cos2, color='green', linestyle=':', label='cos^2(theta)')
plt.plot(theta, y_sum,  color='red', linewidth=3, label='sin^2 + cos^2 (= 1)')

# グラフの見た目調整
plt.title('Pythagorean Theorem in Trigonometry')
plt.xlabel('theta (rad)')
plt.ylabel('Value')
plt.ylim(-0.2, 1.5) # 1の直線を分かりやすくするために上下の幅を調整
plt.grid(True, linestyle=':', alpha=0.6)
plt.legend()
plt.show()
```

**💡 見どころ：** `sin^2` と `cos^2` はそれぞれ波打っているのに、足し算した瞬間、ウソみたいに真っ直ぐな「1の直線」に化ける様子を確認してください。

---

## 3. 波の基本形 $A \sin(2\pi f t)$ とパラメーター

現実世界の電気や機械の振動を扱うとき、私たちは数式をハック（改造）して、回転の大きさやスピードを自由に変える必要があります。その基本形が以下の式です。

$$y = A \sin(2\pi f t)$$

* **$A$（振幅：Amplitude）：** 波の大きさ（ピークの高さ）。$A$ が大きくなると、波のテッペンが高くなります。
* **$f$（周波数：Frequency）：** 1秒間に何回転するかという「回転スピード」。周波数 $f$ が高くなるほど、波はギュッと横に詰まり、激しい振動になります。
* ※「周期（1往復にかかる秒数）」という言葉もありますが、電気や通信の実務ではほぼすべて「周波数 $f$」が主役になるため、今回は周波数をメインに覚えるだけで十分です。

### 💻 Colabデモコード2：パラメーター変更による波の変化

```python
import numpy as np
import matplotlib.pyplot as plt

# 横軸：時間 t (0秒から1秒まで)
t = np.linspace(0, 1, 300)

# 1. ベースとなる波 (振幅A=1.0, 周波数f=2Hz)
y_base = 1.0 * np.sin(2 * np.pi * 2 * t)
plt.plot(t, y_base, color='blue', linewidth=3, label='Base: f=2Hz, A=1.0')

# 2. 周波数を2倍(4Hz)にした波 (回転スピードが2倍速い！)
y_fast = 1.0 * np.sin(2 * np.pi * 4 * t)
plt.plot(t, y_fast, color='red', linewidth=1.5, linestyle='--', label='Fast: f=4Hz (Speed x2)')

# 3. 振幅を1.5倍にした波 (ピークが高くなる！)
y_high = 1.5 * np.sin(2 * np.pi * 2 * t)
plt.plot(t, y_high, color='green', linewidth=1.5, linestyle=':', label='High: f=2Hz, A=1.5')

# グラフの見た目調整
plt.title('Wave Parameters Demo')
plt.xlabel('Time (t)')
plt.ylabel('Value')
plt.grid(True, linestyle=':', alpha=0.6)
plt.legend()
plt.show()

```

---

## 4. 現実への着地：日本のコンセントの秘密

さっき学んだ数式のパラメーターを、そのまま「現実のコンセントの電気（交流電源）」に当てはめてみましょう。

### 4.1 「AC 100V」の大ウソ？

「日本の家庭用コンセントは100V」と誰もが習いますが、実は波のテッペン（ピーク電圧 $A$）は**141V**もあります。
100Vというのは、常に激しく変動するサイン波のエネルギーを平均化（実効値：RMS）した数字に過ぎません。計算式は以下の通りです。

$$\text{ピーク電圧 } A = 100\text{V} \times \sqrt{2} \approx 141.4\text{V}$$

### 4.2 東日本（50Hz）vs 西日本（60Hz）

東日本は1秒間に50回転（50Hz）、西日本は1秒間に60回転（60Hz）のスピードで発電機が回っています。コンセントの電気は、一瞬のうちに進んでしまうため、時間軸を「0秒〜0.05秒（50ミリ秒）」という極小の一瞬に切り取ってPythonで重ね書きしてみます。

### 💻 Colabデモコード3：50Hz と 60Hz の交流電圧

```python
import numpy as np
import matplotlib.pyplot as plt

# 横軸：時間 t (0秒から0.05秒までの「一瞬」を細かく刻む)
t = np.linspace(0, 0.05, 500)

A = 141  # コンセントのピーク電圧 (100V * sqrt(2))

# 1. 東日本の電気 (50Hz)
y_east = A * np.sin(2 * np.pi * 50 * t)

# 2. 西日本の電気 (60Hz) -> 回転スピードが速いので波が詰まる
y_west = A * np.sin(2 * np.pi * 60 * t)

# 重ね書きプロット
plt.figure(figsize=(8, 4))
plt.plot(t, y_east, color='blue', linewidth=2.5, label='East Japan: 50Hz')
plt.plot(t, y_west, color='red',  linewidth=1.5, linestyle='--', label='West Japan: 60Hz')

# 141Vと-141Vのラインを強調
plt.axhline(141, color='gray', linestyle=':', alpha=0.7)
plt.axhline(-141, color='gray', linestyle=':', alpha=0.7)
plt.ylim(-160, 160)

plt.title('Japanese AC Power Supply (100V RMS)')
plt.xlabel('Time (seconds)')
plt.ylabel('Voltage (V)')
plt.grid(True, linestyle=':', alpha=0.6)
plt.legend()
plt.show()

```

### ⚡ 歴史の雑学：なぜ日本だけ真っ二つなのか？

現在進行形で、これほど完璧に国の中で周波数が真っ二つに分かれたまま、どちらも巨大経済圏として共存しているケースは世界中で日本だけです。

原因は明治時代（1890年代）の発電機の輸入先でした。

* **東日本：ドイツから輸入**（1895年、東京電燈がドイツのAEG社から50Hz機を購入）
* **西日本：アメリカから輸入**（1897年、大阪電燈がアメリカのGE社から60Hz機を購入）

後から統一しようとしたものの、すでに街中に電気機器が普及しすぎており、統一コストが数兆〜数十兆円にのぼるため、パンドラの箱として現在まで残されました。
そのため、静岡県の富士川などを境界線に、東西で電気をやり取りするための「巨大な周波数変換所（翻訳機）」が稼働しています。2011年の東日本大震災の際、西日本に電気が余っていたにもかかわらず東日本が計画停電に追い込まれたのは、この「変換所の処理能力の限界」が原因でした。

---

## 5. 【演習】データ合成ミッション（電気＆機械編）

ここからは皆さん自身のプログラミングの時間です。
「2つの異なる波が合流して合成（足し算）されたとき、一体何が起きるのか」を、以下の2つの現実的なトラブル・現象をテーマにプログラムで暴いてみましょう。

### 📝 演習1：【電気編】スタートがズレた電気の合流

ある電気回路で、同じピーク電圧（141V）、同じ周波数（50Hz）の2つの交流電源が合流しました。ただし、2つ目の電源はスタート（始まりの角度）が $120^\circ$（`2 * np.pi / 3` ラジアン）だけ遅れています。
この2つの電気を合成（足し算）したとき、全体の電圧はどうなるでしょうか？ ピークは $141 + 141 = 282\text{V}$ になるでしょうか？

### 📝 演習2：【機械編】ツインモーターの不具合診断（うなり現象）

ある工場で、2台の大型モーターを隣り合わせで運転しています。1台目の振動は **50Hz**、2台目の振動はネジの緩みによってわずかにズレて **52Hz** になってしまいました。
この2台の振動が床で合流したとき、作業員から「ときどきガタガタと周期的に大きな不気味な振動が発生する」と報告が上がりました。合成波を描き、原因を視覚的に説明しなさい。

### 💻 演習用穴埋めコード（Colabにコピペして実行）

以下のコードの `【ナイショ】` の部分（2箇所）に、**波同士を足し算するコード**を追記して、プログラムを完成させてください。

```python
import numpy as np
import matplotlib.pyplot as plt

# ==========================================
# 📊 【演習1：電気編】スタートがズレた電気の合流
# ==========================================
t_elec = np.linspace(0, 0.04, 500) # 0.04秒間（50Hzの波が2周期分入る）

# 1つ目の電源（スタートは0）
y1 = 141 * np.sin(2 * np.pi * 50 * t_elec)

# 2つ目の電源（スタートが 120度 遅れている）
y2 = 141 * np.sin(2 * np.pi * 50 * t_elec - (2 * np.pi / 3))

# 【ミッション1】 2つの電気を合成（足し算）しなさい
y_sum_elec = 【ナイショ】  # ← ★ここを書き換える★

# プロット（自動でグラフが出ます）
plt.figure(figsize=(8, 3.5))
plt.plot(t_elec, y1, color='blue', linestyle=':', label='Source 1')
plt.plot(t_elec, y2, color='green', linestyle=':', label='Source 2 (Delayed)')
plt.plot(t_elec, y_sum_elec, color='red', linewidth=3, label='Combined Voltage')
plt.title('Exercise 1: AC Voltage Synthesis')
plt.xlabel('Time (s)')
plt.ylabel('Voltage (V)')
plt.grid(True, linestyle=':', alpha=0.6)
plt.legend()
plt.show()

print("-" * 50) # 区切り線

# ==========================================
# 📊 【演習2：機械編】ビミョーに違う振動の合流
# ==========================================
t_mech = np.linspace(0, 1.0, 1000) # 1.0秒間を細かく観察

# 1台目のモーターの振動（50Hz）
v1 = 1.0 * np.sin(2 * np.pi * 50 * t_mech)

# 2台目のモーターの振動（52Hz）
v2 = 1.0 * np.sin(2 * np.pi * 52 * t_mech)

# 【ミッション2】 2つの振動を合成（足し算）しなさい
y_sum_mech = 【ナイショ】 # ← ★ここを書き換える★

# プロット（自動でグラフが出ます）
plt.figure(figsize=(8, 3.5))
plt.plot(t_mech, y_sum_mech, color='purple', linewidth=1.5, label='Combined Vibration')
plt.title('Exercise 2: Vibration Interference (Beats)')
plt.xlabel('Time (s)')
plt.ylabel('Displacement')
plt.grid(True, linestyle=':', alpha=0.6)
plt.legend()
plt.show()

```

---

### 💡【時間があれば：さらに深い電気のリアル】「力率（りきりつ）」ってなに？

演習1で驚くべきことが分かりました。141Vの電気と141Vの電気を足したのに、答えは282Vにならず、**元の波と同じ141Vのまま** でした。
これは、2つの波の「スタートの位置（位相）」がズレて合流したため、お互いのエネルギーを引っ張り合って打ち消してしまったからです。

これと全く同じ現象が、皆さんの家や工場のコンセントの奥でも起きています。

#### 電圧と電流の「追いかけっこ」

交流の電気では、「電圧（電気を押し出す力）」**の波と、**「電流（実際に流れる電気の量）」の波の2つが同時に流れています。
理想的には、この2つの波がピッタリ同じタイミング（位相）で流れてくれるのが一番効率が良いです。

しかし、回路の中に「モーター（コイル）」などの部品が入ると、電流の波のスタートが、電圧の波よりもビミョーに遅れてしまう（位相がズレる）という現象が起きます。

#### 力率（Power Factor）＝ 電気の有効活用度

波のスタートがズレると、演習1と同じように、送り込まれた電気エネルギーがお互いに打ち消し合ってしまい、100%の力を発揮できなくなります。
この「送り込まれた電気のうち、どれくらいを無駄にせず有効に使えたか」という割合 **のことを、電気工学では**「力率（りきりつ）」と呼びます。

* **力率 1.0（100%）：** 電圧と電流の波が完全に一致している（最高に効率が良い）。
* **力率 0.5（50%）：** 波がズレているせいで、送った電気の半分がエネルギーとして使われず無駄になっている。

そして、この力率は、電圧と電流の「ズレた角度 $\theta$」を使って、なんと **$\cos(\theta)$** という非常にシンプルな数式で計算されます。
教科書であれほど退屈だった $\cos$ が、電気の世界では「電気代のムダ度を測るための超重要パラメーター」として主役を張っているのです。

---

## 6. 後学のための予備知識：逆関数（アーク関数）

最後に、皆さんがこれから上の学年に進んだときに必ず出会う、強力な武器をチラ見せして終わります。

さっきの「力率（$\cos\theta$）」の話を聞いて、皆さんはこう思ったはずです。
「電圧と電流が何分（何ラジアン）ズレているか（$\theta$）が分からなきゃ、力率が計算できないじゃないか」と。

その通りです。現実のエンジニアリングでは、「角度 $\theta$」をはじめから教えてくれる親切な教科書はありません。手元にあるのは、オシロスコープなどで計測した「ズレた波の値（データ）」だけです。

これまでは「$\sin$ や $\cos$ の土管」の入り口に角度を入れて、出口から値を出していました。
しかし、ここからは逆です。**「手元に波のデータ（値）はある。ここから逆流させて、原因となった『ズレの角度 $\theta$』を暴き出したい！」** という場面に出会います。

土管の出口から値を逆向きに突っ込んで、入り口から元の角度を逆算する「逆流の土管」のことを逆関数（アーク関数）と呼びます。

* $\sin$ の逆流 $\rightarrow$ $\arcsin$（アークサイン）
* $\cos$ の逆流 $\rightarrow$ $\arccos$（アークコサイン）
* $\tan$ の逆流 $\rightarrow$ $\arctan$（アークタンジェント）

教科書や関数電卓によっては、$\sin^{-1}, \cos^{-1}, \tan^{-1}$ と表しています。

データから力率や位相のズレを逆算するとき、この逆関数（特に $\arctan$）が主役として大活躍します。Pythonなら、たった1行でこの逆流の計算が可能です。


### 💻 逆関数の1秒体験コード

```python
import numpy as np

# 例：30°のときの sin の値は 0.5 だった
value = 0.5

# arcsin を使って、値「0.5」から元の角度（ラジアン）を逆算
rad_ans = np.arcsin(value)

# ラジアンを「度（degree）」に変換して表示
deg_ans = np.degrees(rad_ans)

print(f"影の長さが {value} になる元の角度は... {deg_ans:.1f} 度です！")
# 出力：影の長さが 0.5 になる元の角度は... 30.0 度です！

```

数学をただ頭の中で妄想するだけでなく、Pythonという道具を使って「現実のデータ」として扱い、ハックしていく面白さを、これからの学びでもぜひ活かしていってください！
