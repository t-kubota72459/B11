# 第10回・第11回 合同提出課題: 三角関数の工学的応用（電源波形と力率の逆算）

* **本講義の課題は、Google Colab で作成したノートブック (`.ipynb`) を Google Classroom へ提出します。**
* 以下の内容を1つのノートブックにまとめ、期限までに提出してください。
* **テキストセル**と**コードセル**を使って、学生自身の解答や考察がわかりやすくなるようにレイアウトしてください。
* **提出先:** Google Classroom の指定された課題「第10回・第11回合同課題」にファイルを添付して提出。
1. `[ファイル]` ＞ `[ダウンロード]` ＞ `[.ipynb をダウンロード]` を選択。
2. ダウンロードフォルダにある `工業数学_10_11_合同課題_学籍番号_氏名.ipynb` を提出する。

---

## 📊 三角関数を用いた波形解析と工学的診断

いずれの問題も、提示された Python コードを Google Colab に貼り付けて実行し、出力される正確なグラフや計算結果を観察しながら解答すること。

---

### ■ 課題1: 【電気工学】 産業用200Vコンセントの電圧波形シミュレーション

#### 【問題】

授業では、日本の家庭用100Vコンセントの実効値（RMS）と、波のテッペンである「ピーク電圧（最大値 $A$）」の間に以下の関係があることを学んだ。

$$\text{ピーク電圧 } A = \text{実効値} \times \sqrt{2}$$

工場で大型の機械を動かす場合や、家庭でもエアコンやIHクッキングヒーターを使用する場合、より大きなパワーが出せる「単相200V（実効値200V）」の電源が使われる。

1. **【数値計算とグラフ描画】**  
   実効値が $200\text{V}$ であるときのピーク電圧 $A$ の値を計算し、小数点第1位まで求めなさい（$\sqrt{2} \approx 1.414$ として計算するか、Python上で `np.sqrt(2)` を使用してもよい）。また、以下の西日本（$60\text{Hz}$）の200V電源の波形を描画する Python コードの `★ここを書き換える★` に、計算したピーク電圧の数値を入力してグラフを出力させなさい。

2. **【レポート記述】**  
   出力されたグラフの横軸（時間軸：0秒〜0.05秒）を観察し、200Vコンセントの電圧の波が「0秒から0.05秒の間に何往復（何周期）しているか」をグラフから読み取って答えなさい。また、なぜその往復数になるのか、**「周波数 60Hz（1秒間に60往復する）」** という意味の数値を用いて、計算の根拠を数式で示しなさい。

#### 💻 Colab用コード

```python
import numpy as np
import matplotlib.pyplot as plt

# 0秒から0.05秒（50ミリ秒）までの「一瞬」を細かく刻む
t = np.linspace(0, 0.05, 500)

# ★【ミッション】200V（実効値）のピーク電圧 A を入力しなさい（例: 282.8 のような数値）★
A = ★ここを書き換える★

# 西日本の 200V 交流電圧（周波数 f = 60Hz）
y_200v = A * np.sin(2 * np.pi * 60 * t)

# グラフ描画処理
plt.figure(figsize=(8, 4))
plt.plot(t, y_200v, color='red', linewidth=2, label='Industrial AC 200V (60Hz)')
plt.axhline(A, color='gray', linestyle=':')
plt.axhline(-A, color='gray', linestyle=':')
plt.ylim(-320, 320)

plt.title('AC 200V Power Supply Waveform')
plt.xlabel('Time (seconds)')
plt.ylabel('Voltage (V)')
plt.grid(True, linestyle=':', alpha=0.6)
plt.legend()
plt.show()

```

---

### ■ 課題2: 【エネルギー工学】 逆関数（アーク関数）を用いた許容力率の逆算

#### 【問題】

工場の省エネ設計において、電気をどれだけ無駄なく使えているかを表す指標を「力率（りきりつ）」と呼び、電圧の波と電流の波のズレ（位相差 $\theta$）を使って $\cos(\theta)$ で計算される。
この工場では、電気料金の割引特典を受けるために、「力率を 0.85（85%）以上に維持する」という設計目標が掲げられた。

$$\text{力率} = \cos(\theta) \ge 0.85$$

1. **【プログラム実行】**  
   以下の Python コードは、$\cos$ の逆関数である `np.arccos()`（アークコサイン）を使って、力率がぴったり `0.85` になるときの「電圧と電流のズレの角度 $\theta$」を逆算するプログラムである。コードをそのまま Colab に貼り付けて実行し、出力される角度（度数法）を確認しなさい。
2. **【レポート記述】**   
   出力された結果をもとに、この工場で目標を達成するために許される「電圧と電流のズレの角度 $\theta$」の条件を答えなさい（例：「〇〇度以内にする必要がある」など）。また、なぜ「〇〇度以上」ではなく「〇〇度以内（小さく）」でなければならないのか、$\cos(\theta)$ の性質（角度が0度から大きくなっていくとき、値はどう変化するか）を踏まえて理由を説明しなさい。

#### 💻 Colab用コード

```python
import numpy as np

# 目標とする力率
target_power_factor = 0.85

# arccos (アークコサイン) を使って、値から元の角度（ラジアン）を逆算
theta_rad = np.arccos(target_power_factor)

# ラジアンを「度（degree）」に変換
theta_deg = np.degrees(theta_rad)

print(f"--- 診断結果 ---")
print(f"力率が {target_power_factor} になるときのズレの角度は: {theta_deg:.2f} 度です。")

```

---

### ■ 課題3：【自由記述】振り返りレポート

#### 【問題】

今回のように、手計算で数式を解くだけでなく、コンピュータ（Python）を使って「実効値から波形の最大値を割り出して可視化する」ことや「データの測定値から原因となる角度を逆算する」ことには、実際の工学設計やマシントラブルの現場において、どのようなメリットがあると考えられますか。
また、高校までの数学（テストで点数を取るための計算）と、今回の授業で扱った数学（現実の電気回路や規格の設計への応用）を比較して、気づいたことや感じたことを自由に述べよ。
