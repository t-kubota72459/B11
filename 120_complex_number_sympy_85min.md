# 第12回 複素数と位相：サイン波の計算をサボるための「2次元マップ」

### ー SymPy を「賢い電卓」として使い、交流の位相を矢印で考える ー

---

## 🎯 今日のゴール

今日の授業では、次のことを目標にします。

1. **虚数・複素数を「直交する2本目の軸」** として割り切り、2次元マップとして扱えるようになる。
2. 複素数に $j$ を掛ける操作が、**矢印を反時計回りに90度回転させる** ことだと理解する。
3. オイラーの公式 $e^{j\theta}$ を、**角度を指定して矢印を作るボタン** として使えるようになる。
4. 交流回路の電流をフェーザとして表し、**電流計の値が単純な足し算では決まらない** ことを確認する。

---

## 0. Colab の準備

Google Colab を開き、最初のセルに次のコードを入力して実行します。

```python
import sympy as sp

# 電気工学では虚数単位を j と書く
# SymPyでは虚数単位が I と表示されるので、ここでは j = sp.I として使う
j = sp.I
```

> SymPy の表示では `I` と出ることがあります。  
> ただし、この授業では **電気工学の習慣に合わせて $j$ と読む** ことにします。

---

## 1. 【ぶっちゃけトーク】なぜ電気の現場で「複素数」が必要なのか？

交流の電圧や電流は、時間とともに大きさが変わります。

たとえば、次のような2つのサイン波を足し合わせることを考えます。

$$
141\sin(2\pi \cdot 50t)
+
141\sin\left(2\pi \cdot 50t - \frac{2}{3}\pi\right)
$$

この式をそのまま計算しようとすると、三角関数の加法定理を使う必要があります。  
つまり、かなり面倒です。

そこでエンジニアは、次のように考えます。

> サイン波は、回転する矢印の影として見られる。  
> それなら、波の形のまま計算するより、**矢印として足し算したほうが楽** ではないか？

この「矢印を2次元マップ上で扱う道具」が複素数です。

```mermaid
flowchart LR
    A[時間で変化するサイン波] --> B[時間を一時停止する]
    B --> C[複素平面上の矢印にする]
    C --> D[矢印の足し算・引き算で計算する]
    D --> E[最後に大きさや位相を読む]
```

※ 電気工学では、電流の記号 $i$ と混ざらないように、虚数単位を $i$ ではなく **$j$** と書くことが多いです。

---

## 2. 【割り切りその1】複素平面は、ただの「2軸のマップ」である

複素数は、次のような2つの成分を持つ数です。

$$
z = a + bj
$$

ここで、

- $a$：横方向の成分
- $b$：縦方向の成分
- $j$：縦方向を表すための記号

だと考えます。

つまり、

$$
3 + 4j
$$

は、

> 右に3、上に4進んだ矢印

と考えればよいです。

---

### $j$ を掛けると、90度回転する

複素数の強力な性質はこれです。

> **$j$ を掛けると、矢印が反時計回りに90度回転する。**

たとえば、右向きの矢印 $1$ に $j$ を掛けると、上向きの矢印になります。

$$
1 \times j = j
$$

さらにもう一度 $j$ を掛けると、

$$
j \times j = j^2 = -1
$$

となり、左向きの矢印になります。

---

### 💻 ワーク1：SymPyで $j$ の回転を確認する

次のコードを Colab に貼り付けて実行しなさい。

```python
import sympy as sp
j = sp.I

z0 = 1
z1 = z0 * j
z2 = z1 * j
z3 = z2 * j
z4 = z3 * j

print("最初       :", z0)
print("jを1回掛ける:", z1)
print("jを2回掛ける:", z2)
print("jを3回掛ける:", z3)
print("jを4回掛ける:", z4)
```

#### 確認しよう

- 1回掛けると、どの向きになるか。
- 2回掛けると、どの向きになるか。
- 4回掛けると、どこに戻るか。

---

## 3. 【割り切りその2】オイラーの公式は「矢印を作るボタン」

オイラーの公式は、次の式です。

$$
e^{j\theta} = \cos\theta + j\sin\theta
$$

この式を、今回の授業では次のように割り切ります。

> $e^{j\theta}$ は、角度 $\theta$ の向きに、長さ1の矢印を作るボタンである。

証明はここでは扱いません。  
まずは、**角度を入れたら、その向きの矢印が出てくる** という使い方を覚えます。

---

### 💻 ワーク2：SymPyでオイラーの矢印を作る

次のコードを Colab に貼り付けて実行しなさい。  
`theta_deg` の値を `0`, `30`, `45`, `90`, `180` などに変えて、結果を確認しなさい。

```python
import sympy as sp
j = sp.I

# ここの角度を変えてみる
# 例：0, 30, 45, 90, 180
theta_deg = 45

# 度をラジアンに変換
theta = theta_deg * sp.pi / 180

# オイラーのボタン
arrow = sp.exp(j * theta)

# 実部と虚部を取り出す
x = sp.re(arrow)
y = sp.im(arrow)

print("角度:", theta_deg, "度")
print("矢印:", sp.simplify(arrow))
print("横方向:", sp.simplify(x))
print("縦方向:", sp.simplify(y))
print("小数で見ると:", sp.N(x), ",", sp.N(y))
```

### 角度と矢印の対応

| 角度 | 複素数の矢印 | 向き |
|---:|---|---|
| $0^\circ$ | $1$ | 右 |
| $90^\circ$ | $j$ | 上 |
| $180^\circ$ | $-1$ | 左 |
| $270^\circ$ | $-j$ | 下 |

---

### 🔎 図でも確認したい場合

次のコードは、矢印を図にするためのものです。  
コードの細かい意味は説明しません。**角度を変えて実行し、矢印の向きを見る** ことが目的です。

```python
import sympy as sp
import matplotlib.pyplot as plt

j = sp.I

theta_deg = 45
theta = theta_deg * sp.pi / 180
arrow = sp.exp(j * theta)

x = float(sp.N(sp.re(arrow)))
y = float(sp.N(sp.im(arrow)))

plt.figure(figsize=(4, 4))
plt.quiver(0, 0, x, y, angles='xy', scale_units='xy', scale=1)
plt.xlim(-1.2, 1.2)
plt.ylim(-1.2, 1.2)
plt.axhline(0, color='black', linewidth=0.5)
plt.axvline(0, color='black', linewidth=0.5)
plt.grid(True, linestyle=':')
plt.gca().set_aspect('equal', adjustable='box')
plt.title(f"Euler arrow: {theta_deg} deg")
plt.show()
```

---

## 4. 【本日のメイン】コンデンサを足したのに、電流計の値が小さくなる？

ここからは、交流回路の電流を複素数で考えます。

工場などで使うモータの電流は、電圧と同じタイミングの成分と、電圧より90°遅れた成分に分けて考えることができます。
電流計が指す値は、この2つの成分を合わせた矢印の長さ、つまり複素数の絶対値です。
 
しかし、コンデンサを並列につなぐと、コンデンサには「進んだ電流」が流れます。

すると、遅れた成分を一部打ち消すことができます。

---

### 問題：モータにコンデンサをつないだら？

あるモータに流れる電流を、複素数で次のように表す。

$$
\dot{I}_{motor} = 6 - 8j \quad [\mathrm{A}]
$$

ここで、

- $6$：電圧と同じタイミングで流れる成分
- $-8j$：電圧より $90^\circ$ 遅れて流れる成分

と考える。

このモータにコンデンサを並列につないだところ、コンデンサに次の電流が流れた。

$$
\dot{I}_{C} = 5j \quad [\mathrm{A}]
$$

次の値を求めなさい。

1. コンデンサをつなぐ前の電流計の値
2. コンデンサをつないだ後の全体の電流 $\dot{I}_{total}$
3. コンデンサをつないだ後の電流計の値
4. 電流計の値は増えたか、減ったか

---

### ❌ よくある大間違い

「モータに電流が流れていて、さらにコンデンサにも電流が流れるのだから、電流計の値は増えるはず」

と思うかもしれません。

しかし、交流では電流の**大きさ**だけでなく、**向き・位相**が大切です。  
数直線上で足すのではなく、2次元マップ上の矢印として足します。

---

### ⭕ 複素数マップで考える

モータの電流は、

$$
\dot{I}_{motor} = 6 - 8j
$$

コンデンサの電流は、

$$
\dot{I}_{C} = 5j
$$

したがって、全体の電流は、

$$
\dot{I}_{total}
=
\dot{I}_{motor} + \dot{I}_{C}
$$

$$
\dot{I}_{total}
=
(6 - 8j) + 5j
=
6 - 3j
$$

つまり、コンデンサをつないだ後の電流は、

> 右に6、下に3

の矢印になります。

```mermaid
flowchart LR
    A[モータ電流 6 - 8j] --> C[全体電流 6 - 3j]
    B[コンデンサ電流 +5j] --> C
    C --> D[電流計の値は 矢印の長さ]
```

---

### 💻 ワーク3：SymPyで電流計の値を求める

次のコードを Colab に貼り付けて実行しなさい。

```python
import sympy as sp
j = sp.I

# モータの電流
I_motor = 6 - 8*j

# コンデンサの電流
I_C = 5*j

# 全体の電流
I_total = I_motor + I_C

# 電流計の値 = 複素数の大きさ
I_before = sp.Abs(I_motor)
I_after = sp.Abs(I_total)

print("モータの電流 I_motor =", I_motor)
print("コンデンサの電流 I_C =", I_C)
print("全体の電流 I_total =", I_total)
print()
print("コンデンサをつなぐ前の電流計の値 =", I_before, "A")
print("コンデンサをつないだ後の電流計の値 =", I_after, "A")
print()
print("小数で表示すると")
print("つなぐ前 =", sp.N(I_before), "A")
print("つないだ後 =", sp.N(I_after), "A")
```

---

### 🎯 手計算での答え合わせ

コンデンサをつなぐ前は、

$$
|\dot{I}_{motor}|
=
\sqrt{6^2 + (-8)^2}
=
\sqrt{36 + 64}
=
10
$$

なので、電流計の値は、

$$
10\mathrm{A}
$$

である。

コンデンサをつないだ後は、

$$
\dot{I}_{total} = 6 - 3j
$$

なので、

$$
|\dot{I}_{total}|
=
\sqrt{6^2 + (-3)^2}
=
\sqrt{45}
=
3\sqrt{5}
\approx 6.7
$$

したがって、電流計の値は、

$$
約6.7\mathrm{A}
$$

になる。

---

### ここで大事なこと

コンデンサにも電流は流れています。  
それなのに、全体の電流計の値は、

$$
10\mathrm{A} \rightarrow 約6.7\mathrm{A}
$$

に小さくなりました。

これは、交流の電流が単なる数の足し算ではなく、

> **向きを持った矢印の足し算**

だからです。

複素数を使うと、このような交流回路の計算を、2次元マップ上の矢印として扱うことができます。

---

## 5. 確認問題（時間があれば）

次の3つの場合について、コンデンサをつないだ後の全体の電流と、電流計の値を求めなさい。

モータの電流は同じく、

$$
\dot{I}_{motor} = 6 - 8j
$$

とする。

### 問1

$$
\dot{I}_{C} = 3j
$$

### 問2

$$
\dot{I}_{C} = 8j
$$

### 問3

$$
\dot{I}_{C} = 10j
$$

---

### 💻 確認問題用コード

`I_C = ...` の部分だけを書き換えて確認しなさい。

```python
import sympy as sp
j = sp.I

I_motor = 6 - 8*j

# ここだけ変える
I_C = 3*j

I_total = I_motor + I_C
I_after = sp.Abs(I_total)

print("全体の電流 I_total =", I_total)
print("電流計の値 =", I_after, "A")
print("小数で表示すると", sp.N(I_after), "A")
```

---

## 6. まとめ

今日の授業で押さえることは、次の4つです。

1. 複素数は、横軸と縦軸を持つ **2次元マップ** として考えればよい。
2. $j$ を掛けると、矢印が **反時計回りに90度回転** する。
3. $e^{j\theta}$ は、角度 $\theta$ の向きに矢印を作る **便利なボタン** として使える。
4. 交流回路の電流は、複素数を使うと **フェーザの足し算** として扱える。

---

## 巻末：確認問題の答え

### 問1

$$
\dot{I}_{C} = 3j
$$

$$
\dot{I}_{total} = (6 - 8j) + 3j = 6 - 5j
$$

$$
|\dot{I}_{total}| = \sqrt{6^2 + (-5)^2} = \sqrt{61} \approx 7.81\mathrm{A}
$$

---

### 問2

$$
\dot{I}_{C} = 8j
$$

$$
\dot{I}_{total} = (6 - 8j) + 8j = 6
$$

$$
|\dot{I}_{total}| = 6\mathrm{A}
$$

遅れ成分がちょうど打ち消された。

---

### 問3

$$
\dot{I}_{C} = 10j
$$

$$
\dot{I}_{total} = (6 - 8j) + 10j = 6 + 2j
$$

$$
|\dot{I}_{total}| = \sqrt{6^2 + 2^2} = \sqrt{40} \approx 6.32\mathrm{A}
$$

コンデンサの効果が大きすぎて、今度は進み成分が残った。
