# 第13回 ベクトル：向きと大きさで「現場の量」を扱う

### ー 座標・移動・力・仕事を、矢印としてまとめて計算する ー

---

## ⏱ 授業時間の目安（100分授業・正味85分）

| 時間 | 内容 |
|---:|---|
| 0〜5分 | 今日のゴール、前回の複素数とのつながり |
| 5〜20分 | ベクトルは「向きと大きさを持つ量」 |
| 20〜35分 | 座標・移動量・距離を Colab で計算 |
| 35〜55分 | 内積：同じ向きの成分を取り出す |
| 55〜70分 | クレーンの仕事計算：$W=\vec{F}\cdot\vec{d}$ |
| 70〜85分 | 第12・13回 合同提出課題の説明・作業 |

---

## 🎯 今日のゴール

今日の授業では、次のことを目標にします。

1. ベクトルを「数字のセット」ではなく、**向きと大きさを持つ矢印**として扱えるようになる。
2. 2点間の移動量や距離を、座標から計算できるようになる。
3. 内積を「同じ向きにどれだけ効いているかを調べる計算」として使えるようになる。
4. クレーンや台車の仕事計算を、Google Colab を使って確認できるようになる。

---

## 0. 前回とのつながり：複素数もベクトルも「矢印」だった

前回は、複素数を使って交流電流を矢印として扱いました。

たとえば、

$$
\dot{I}=6-8j
$$

は、横に6、縦に-8進む矢印として見ることができました。

今回のベクトルも考え方はかなり近いです。

$$
\vec{a}=\begin{bmatrix}6\\-8\end{bmatrix}
$$

これは、横に6、縦に-8進む矢印です。

```mermaid
flowchart LR
    A[複素数] --> C[2次元の矢印]
    B[ベクトル] --> C
    C --> D[長さ・向き・足し算ができる]
```

違いは、複素数は「回転」や「位相」を扱うのが得意で、ベクトルは「位置・移動・力」を扱うのが得意ということです。

---

## 1. ベクトルは「方向つきの量」である

普通の数は、大きさだけを表します。

例：

$$
5\mathrm{N}
$$

これは「5ニュートンの力」という大きさだけです。

しかし、力には向きがあります。

- 右向きに5N
- 左向きに5N
- 斜め上に5N

これらは全部ちがう力です。

そこで、向きと大きさをまとめて表す道具としてベクトルを使います。

$$
\vec{F}=\begin{bmatrix}3\\4\end{bmatrix}
$$

これは、

- 右向きに3
- 上向きに4

進む矢印です。

矢印の長さは、三平方の定理で求められます。

$$
|\vec{F}|=\sqrt{3^2+4^2}=5
$$

---

## 2. Google Colab の準備

今回から、ベクトルや行列の計算では `NumPy` を使います。

細かい文法を覚える必要はありません。今回は、次のように使えばよいです。

```python
import numpy as np
```

これは、

> NumPyという計算道具を使う準備

です。

ベクトルは、次のように書きます。

```python
import numpy as np

F = np.array([3, 4])
print(F)
```

---

## 3. ワーク1：力の大きさを求める

次の力を考えます。

$$
\vec{F}=\begin{bmatrix}3\\4\end{bmatrix}\mathrm{N}
$$

この力の大きさを求めましょう。

```python
import numpy as np

F = np.array([3, 4])

F_length = np.linalg.norm(F)

print("力ベクトル F =", F)
print("力の大きさ =", F_length, "N")
```

実行すると、力の大きさは

$$
5\mathrm{N}
$$

になります。

### ✅ ここで確認

`np.linalg.norm(F)` は、ベクトルの長さを求めるための関数です。

手計算では、

$$
\sqrt{3^2+4^2}
$$

を計算しています。

---

## 4. ワーク2：2点間の距離を求める

機械加工では、穴あけ位置や部品の取り付け位置を座標で表すことがあります。

たとえば、板金上に2つの穴 A, B があるとします。

$$
A=(10,20)
$$

$$
B=(70,65)
$$

単位は mm とします。

このとき、AからBへ向かうベクトルは、

$$
\vec{AB}=B-A
$$

です。

```python
import numpy as np

A = np.array([10, 20])
B = np.array([70, 65])

AB = B - A
AB_length = np.linalg.norm(AB)

print("A =", A, "mm")
print("B =", B, "mm")
print("AからBへの移動量 AB =", AB, "mm")
print("AとBの距離 =", AB_length, "mm")
```

### 🎯 読み取り

出力された

```text
AB = [60 45]
```

は、

- 右に60mm
- 上に45mm

移動するという意味です。

距離は、

$$
\sqrt{60^2+45^2}=75
$$

なので、75mmです。

---

## 5. 内積：同じ向きにどれだけ効いているか

ベクトルには、足し算や長さだけでなく、**内積**という便利な計算があります。

2つのベクトル

$$
\vec{a}=\begin{bmatrix}a_x\\a_y\end{bmatrix},\quad
\vec{b}=\begin{bmatrix}b_x\\b_y\end{bmatrix}
$$

の内積は、

$$
\vec{a}\cdot\vec{b}=a_xb_x+a_yb_y
$$

です。

Google Colab では、

```python
np.dot(a, b)
```

で計算できます。

ただし、式の形だけを見ると意味がわかりにくいです。

工業数学では、内積を次のように考えます。

> 内積は、片方のベクトルが、もう片方の向きにどれだけ効いているかを調べる計算である。

---

## 6. ワーク3：力が移動方向にどれだけ効いているか

台車を斜め上に引っ張る場面を考えます。

力は、

$$
\vec{F}=\begin{bmatrix}40\\30\end{bmatrix}\mathrm{N}
$$

移動は、

$$
\vec{d}=\begin{bmatrix}5\\0\end{bmatrix}\mathrm{m}
$$

とします。

つまり、台車は右向きに5mだけ動きます。

```python
import numpy as np

F = np.array([40, 30])  # N
d = np.array([5, 0])    # m

W = np.dot(F, d)

print("力 F =", F, "N")
print("移動 d =", d, "m")
print("仕事 W =", W, "J")
```

結果は、

$$
W=200\mathrm{J}
$$

です。

### なぜ $30\mathrm{N}$ の成分は効かないのか？

力のうち、上向きの30Nは、今回の「右向きの移動」には直接効いていません。

右向きに動いた距離はありますが、上向きには動いていないからです。

したがって、仕事は

$$
W=40\times 5+30\times 0=200
$$

になります。

```mermaid
flowchart LR
    F[斜めの力] --> X[右向き成分]
    F --> Y[上向き成分]
    X --> W[移動方向に効く]
    Y --> N[今回は仕事に効かない]
```

---

## 7. ワーク4：角度から仕事を求める

力の大きさが50Nで、移動方向となす角が60度だったとします。

移動距離は4mです。

このとき、仕事は

$$
W=Fd\cos\theta
$$

で求められます。

これは内積の式そのものです。

```python
import numpy as np

F_length = 50   # N
d_length = 4    # m
theta_deg = 60  # 度

W = F_length * d_length * np.cos(np.radians(theta_deg))

print("仕事 W =", W, "J")
```

60度の向きに引っ張ると、全部の力が移動方向に使われるわけではありません。

力のうち、移動方向に効くのは

$$
F\cos\theta
$$

の部分だけです。

---

## 8. まとめ

今日の内容を整理します。

| 内容 | 意味 | Colabでの計算 |
|---|---|---|
| ベクトル | 向きと大きさを持つ量 | `np.array([x, y])` |
| 長さ | 矢印の大きさ | `np.linalg.norm(a)` |
| 差 | 2点間の移動量 | `B - A` |
| 内積 | 同じ向きにどれだけ効いているか | `np.dot(a, b)` |
| 仕事 | 力と移動の内積 | `W = np.dot(F, d)` |

---

# 第12・13回 合同提出課題

## 提出物

Google Colab で作成した `.ipynb` ファイルを提出すること。

ファイル名の例：

```text
学籍番号_氏名_第12_13回合同課題.ipynb
```

---

## 課題A：複素数による交流電流の計算（第12回）

あるモータの電流は、次の複素数で表される。

$$
\dot{I}_{motor}=8-6j\quad [\mathrm{A}]
$$

このモータにコンデンサを並列に接続したところ、コンデンサ電流は、

$$
\dot{I}_{C}=4j\quad [\mathrm{A}]
$$

となった。

次の値を Colab で求めなさい。

1. コンデンサ接続前の電流計の値
2. コンデンサ接続後の全体電流
3. コンデンサ接続後の電流計の値
4. 電流計の値が増えたか、減ったか

### 参考コード

```python
import sympy as sp

j = sp.I

I_motor = 8 - 6*j
I_C = 4*j

I_total = I_motor + I_C

print("接続前の電流 =", sp.Abs(I_motor), "A")
print("接続後の全体電流 =", I_total, "A")
print("接続後の電流 =", sp.N(sp.Abs(I_total)), "A")
```

---

## 課題B：ベクトルによる仕事の計算（第13回）

台車に次の力を加えた。

$$
\vec{F}=\begin{bmatrix}120\\50\end{bmatrix}\mathrm{N}
$$

台車は次のように移動した。

$$
\vec{d}=\begin{bmatrix}3\\0\end{bmatrix}\mathrm{m}
$$

次の値を Colab で求めなさい。

1. 力ベクトルの大きさ
2. 移動ベクトルの大きさ
3. 仕事 $W=\vec{F}\cdot\vec{d}$
4. 上向きの50N成分が仕事に効いているかどうかを、1〜2文で説明しなさい。

### 参考コード

```python
import numpy as np

F = np.array([120, 50])
d = np.array([3, 0])

print("力の大きさ =", np.linalg.norm(F), "N")
print("移動距離 =", np.linalg.norm(d), "m")
print("仕事 =", np.dot(F, d), "J")
```

---

## レポートに書くこと

Colab の Markdown セルに、次の内容を書くこと。

1. 計算の目的
2. 使用した式
3. Colab の実行結果
4. 結果の説明

### 書き方の例

```text
上向きの力成分は存在するが、台車は水平方向にしか移動していない。
そのため、仕事に寄与するのは水平方向の力成分だけである。
Colabで内積を計算した結果、仕事は360Jであることを確認した。
```

「〜だと思う」ではなく、「〜を確認した」「〜である」と書きましょう。
