# 第13回 ベクトル：向きと大きさで「現場の量」を扱う

### 💡 座標・移動・力・仕事を、矢印としてまとめて計算する 💡

---

## ⏱ 授業時間の目安（100分授業・正味85分）

| 時間 | 内容 |
|---:|---|
| 0〜5分 | 今日のゴール、前回の複素数とのつながり（どちらも矢印？） |
| 5〜15分 | ベクトルは「向きと大きさを持つ量」（3次元への拡張） |
| 15〜35分 | 💥【重要】ベクトルの3大基本演算（足し算・スカラー倍・引き算） |
| 35〜50分 | 座標・移動量・距離を Colab（`np.linalg.norm`）で計算 |
| 50〜70分 | 内積：同じ向きの成分を取り出す「協力度メーター」と仕事の計算 |
| 70〜85分 | 第12・13回 合同提出課題の説明・作業 |

---

## 🎯 今日のゴール

今日の授業の目標は以下の4つです。

1. ベクトルを「数字のセット」ではなく、 **向きと大きさを持つリアルな矢印** として扱えるようになる。
2. ベクトルの **足し算・スカラー倍・引き算** の意味を図とPythonの両方でマスターする。
3. 2点間の移動量や距離を、座標の引き算から一発で計算できるようになる。
4. 内積を使い、クレーンや台車の仕事（エネルギー）の計算を自動化できるようになる。

---

## 0. 前回とのつながり：複素数もベクトルも「矢印」だけど何が違う？

前回は、複素数を使って交流電流を矢印として扱いました。たとえば、モータに流れる電流を $\dot{I} = 6 - 8j$ A のように表しましたね。
今回のベクトルも、見た目はそっくりな「矢印」です。

$$\vec{a} = \begin{bmatrix} 6 \\ -8 \end{bmatrix}$$

形は似ていますが、工学の世界ではこの2つを明確に使い分けます。

* **複素数（電気で大活躍）：** 電線は1本しかありません（空間的には1次元）。それなのに矢印を使うのは、 **「波のタイミング（時間・位相）のズレ」** を回転で表したいからです。
* **ベクトル（力学やロボットで大活躍）：** こちらはリアルな **「2次元平面や3次元空間の向き」** そのものを表します。右に6mm、下に8mm進む、というような物理的な空間を扱います。

## かんたん問題：
街をさまよっていたら迷子になった。最初にいた場所を原点 (0, 0) としたとき、キミはどこにいるだろうか。

1. 北へ 3 km 進み
2. 東へ 3 km 進み
3. 南へ 1 km 進み、
4. 南西へ $\sqrt{2}$ km 進み
5. 東に 2 km 進んだ。

結局どの地点か。

---

## 1. ベクトルは「方向つきの量」である

「5 N（ニュートン）」という数字だけでは、力がどちらに働いているか分かりません。
「右向きに5 N」と「上向きに5 N」では、機械の動きは全く変わってしまいます。そこで、向きと大きさをセットで表すためにベクトルを使います。

$$\vec{F} = \begin{bmatrix} 3 \\ 4 \end{bmatrix}$$

これは、**「右（x軸）向きに3、上（y軸）向きに4」** の力で引っ張っている、という状態を表す矢印です。
この矢印の純粋な「引っ張る強さ（長さ）」は、先週やった三平方の定理で求められます。

$$|\vec{F}| = \sqrt{3^2 + 4^2} = 5\text{ N}$$

### 📌 3次元（空間）になってもルールは同じ！

ドローンが、東へ 3 km、北へ 4 km、上空へ 5 km 移動したとします。
このときのドローンの直線移動距離（矢印の長さ）はどうなるでしょうか？ 実は、3次元になってもピタゴラスの定理に $z^2$ を付け足すだけでそのまま通用します！

$$L = \sqrt{3^2 + 4^2 + 5^2} = \sqrt{50} \approx 7.07\text{ km}$$

これらはすべて、Pythonでは **「ノルム頂戴！」（`np.linalg.norm`）** という1行の命令で一発計算できます。

---

## 2. ベクトルの3大基本演算（足し算・スカラー倍・引き算）

ここが今日の心臓部です。ベクトルは、普通の数字（1, 2, 3...）とは少し違う計算ルールを持っています。ですが、**Pythonを使うと、普通の数字と全く同じ感覚で計算できてしまいます。**

まずは準備として、NumPyをインポートしておきましょう。

```python
import numpy as np
```

---

### ① 足し算（$\vec{a} + \vec{b}$）：力を合わせる・続けて進む

* **図のイメージ：** 1つ目の矢印の「先端」に、2つ目の矢印の「お尻」を **つなぎ合わせる** 。スタートから最終ゴールまでをダイレクトに結んだ新しい矢印が、足し算の結果です。
* **工学的意味：** 2つの力を合わせた「合力」や、続けて移動したときの「合計移動ルート」。
* **計算ルール：** x同士、y同士をそれぞれ普通に足すだけ。

$$\begin{bmatrix} 2 \\ 5 \end{bmatrix} + \begin{bmatrix} 4 \\ -1 \end{bmatrix} = \begin{bmatrix} 2+4 \\ 5+(-1) \end{bmatrix} = \begin{bmatrix} 6 \\ 4 \end{bmatrix}$$

```python
# Pythonならそのまま「+」でOK！
a = np.array([2, 5])
b = np.array([4, -1])
print("足し算 a + b =", a + b)  # 出力: [6 4]
```

---

### ② スカラー倍（$k\vec{a}$）：向きはそのまま、長さだけ変える

* **図のイメージ：** 矢印の向き（角度）は一切変えずに、**長さだけを2倍、3倍にビヨーンと引き伸ばす**計算です。
* **マイナスを掛けると？：** $-1$ を掛けると、長さはそのままで「真逆」を向く矢印に大反転します。
* **計算ルール：** すべての成分に、その数字（スカラー）を掛け算する。

$$3 \times \begin{bmatrix} 2 \\ 4 \end{bmatrix} = \begin{bmatrix} 3 \times 2 \\ 3 \times 4 \end{bmatrix} = \begin{bmatrix} 6 \\ 12 \end{bmatrix}$$

```python
# Pythonならそのまま「*」で掛け算！
a = np.array([2, 4])
print("3倍にする   =", 3 * a)   # 出力: [ 6 12]
print("真逆にする (-1倍) =", -1 * a)  # 出力: [-2 -4]
```

---

### ③ 引き算（$\vec{b} - \vec{a}$）：現在地から目的地へのワープ

一番つまずきやすいのが引き算です。図的な意味をしっかり脳に焼き付けましょう。

* **図のイメージ：** 2つの矢印の「お尻（スタート地点）」を揃えて置きます。このとき、「$\vec{a}$ の先端から、$\vec{b}$ の先端へ向かって引いた矢印」が、引き算（$\vec{b} - \vec{a}$）の結果です。
* **超重要フレーズ：** ベクトルの引き算は常に **「後（ゴール）ひく前（スタート）」** と覚えましょう。$\vec{a}$（現在地）から $\vec{b}$（目的地）へ行くにはどう動けばいいか？ というギャップを表します。
* **計算ルール：** x同士、y同士をそれぞれ引く。

$$\begin{bmatrix} 5 \\ 7 \end{bmatrix} - \begin{bmatrix} 1 \\ 4 \end{bmatrix} = \begin{bmatrix} 5-1 \\ 7-4 \end{bmatrix} = \begin{bmatrix} 4 \\ 3 \end{bmatrix}$$

```python
# Pythonならそのまま「-」でOK！
a = np.array([1, 4])  # スタート地点
b = np.array([5, 7])  # ゴール地点
print("引き算 b - a =", b - a)  # 出力: [4 3] （右に4、上に3進めばワープできる！）
```

---

## 3. ワーク1：加工位置（2点間）の移動量と距離を求める

基本がわかれば、設計現場の実務データも怖くありません。
板金の上に、穴Aと穴Bの2つの加工点があるとします（単位：mm）。

$$A = (10, 20),\quad B = (70, 65)$$

穴A（現在地）から穴B（目的地）へ向かう移動ベクトル $\vec{AB}$ を出して、その直線距離を測ってみましょう。合言葉は「後（B）ひく前（A）」です。

```python
import numpy as np

A = np.array([10, 20]) # スタート
B = np.array([70, 65]) # ゴール

# 移動ベクトル（引き算）
AB = B - A

# 直線距離（ノルム）
AB_length = np.linalg.norm(AB)

print("AからBへの移動ベクトル AB =", AB, "mm")
print("穴と穴の間の直線距離       =", AB_length, "mm")
```

#### 🎯 出力の読み取り

実行すると `AB = [60, 45]` と出てきます。これは「右に60mm、上に45mm進む矢印」という意味です。
直線距離は $\sqrt{60^2 + 45^2} = 75$ mm となり、関数電卓を叩かなくても正確な設計寸法が手に入ります。

---

## 4. 内積（`np.dot`）：自分の力が「同じ向き」にどれだけ効いているか？

ベクトルには、掛け算の親戚のような **「内積（ないせき）」** という計算があります。

$$\vec{a} \cdot \vec{b} = a_x b_x + a_y b_y$$

数式だけ見ると「x同士、y同士を掛けて足す」という謎の作業に見えますが、工業数学における内積の意味はとてもシンプルです。

> **「内積とは、片方の矢印が、もう片方の進む向きに対して『どれだけ協力しているか（効いているか）』を測るメーターである」**

---

### 💻 ワーク2：斜めに引っ張る力と「仕事」のリアル

台車をロープで斜め上に引っ張って、右側に 5 m 移動させる場面を考えます。

* 加えた力：$\vec{F} = \begin{bmatrix} 40 \\ 30 \end{bmatrix}$ N （右に40 N、上に30 N）
* 移動した距離：$\vec{d} = \begin{bmatrix} 5 \\ 0 \end{bmatrix}$ m （右に5 m、上には0 m）

物理や機械の設計では、$\text{力} \times \text{移動距離}$ を **「仕事 $W$（単位：J・ジュール）」** と呼びます。
これをNumPyの内積関数 `np.dot(a, b)` で計算してみましょう。

```python
import numpy as np

F = np.array([40, 30])  # 力 [N]
d = np.array([5, 0])    # 移動 [m]

# 内積（dot積）を計算
W = np.dot(F, d)

print("力ベクトル F =", F, "N")
print("移動ベクトル d =", d, "m")
print("おこなった仕事 W =", W, "J")
```

#### ❓ なぜ上向きの 30 N は「仕事」に効かないのか？

結果は `200 J` になります。
計算をバラすと、$W = (40 \times 5) + (30 \times 0) = 200 + 0 = 200$ J です。

上向きに引っ張っている 30 N の力は、台車をフワッと軽く（浮かせる方向に）しているだけで、**台車を右に進めるという目的（仕事）には1ミリも貢献していません。** だから $30 \times 0$ で消滅します。
このように、「違う向きの成分を無視し、同じ向きに効いたエネルギーだけを賢く抜き出す」のが内積の役割です。

### 💻 ワーク3：マッチングアプリの裏側

内積は実はベクトルの **「一致度」** でもあります。

あなたは新しいマッチングアプリ「アイノベクトル」の開発者です。
ユーザーの「週末の理想の過ごし方」を、次の 2次元ベクトル で表して相性を計算することにしました。
要素は **（アクティブ度, まったり度）** の2つで、それぞれ1〜5点で表されます。

* **アクティブ度**: 外に出かけたい、旅行、スポーツ、カフェ巡りなど
* **まったり度**: 家で映画、ゲーム、読書、お昼寝など

**ユーザーAのベクトル**
$$\vec{u} = (4, 2)$$

**パートナー候補よしこさんのベクトル**

$$\vec{v} = (5, 1)$$

**パートナー候補花子さんのベクトル**
$$\vec{w} = (2, 5)$$

### ❓ 問題

内積 $\vec{u} \cdot \vec{v}$ と $\vec{u} \cdot \vec{w}$ をそれぞれ計算し、AIとしてユーザーAさんにどちらのパートナーを強くおすすめすべきか判定してください。

## 🧠 内積の本質

データサイエンスの世界における内積の本質は「一致度判定」です。

> * **お互いに熱量が高い成分（5と5など）** が掛け合わさると、掛け算のマジックで数値がドカンと跳ね上がります。
> * 逆に、**片方でも興味がない成分（1など）** が混ざると、掛け算のせいで数値が全く伸びません。
 
つまり、内積を計算するだけで、「お互いのこだわりポイントがどれだけ同じ方向を向いているか」が1つの数字として綺麗に炙り出されるわけです。実際のAIは、これを何万次元という壮大なベクトルで一瞬で計算しています。

---

## 5. まとめ

今日登場したNumPyの武器を整理しておきましょう。

| 工学的な意味 | 数学の呼び名 | Pythonでの書き方 |
| --- | --- | --- |
| **向きと大きさを持つ矢印** | ベクトル | `a = np.array([x, y])` |
| **矢印の純粋な長さ（強さ）** | ノルム（大きさ） | `np.linalg.norm(a)` |
| **2つの力を合わせる・連続移動** | ベクトルの和 | `a + b` |
| **向きはそのまま、長さを変える** | スカラー倍 | `3 * a` または `-1 * a` |
| **現在地から目的地への移動量** | ベクトルの差 | `B - A` （後ひく前） |
| **同じ向きにどれだけ効いているか、どれだけ一致しているか** | 内積（ドット積） | `np.dot(a, b)` |
