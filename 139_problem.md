# 🎓 第12・13回 合同提出課題

## ⚠️ 提出ルール

Google Colab で作成した `.ipynb` ファイルを Google Classroom へ提出してください。

* **ファイル名例：** `学籍番号_氏名_第12_13回合同課題.ipynb`
* **記述の注意点：** コースポリシー通り、レポートの考察に「〜だと思う」という主観的な表現は使わないこと。「〜を確認した」「〜である」と事実を客観的に記述してください。

---

## 📝 課題A：複素数による交流電流の計算（第12回の復習）

ある工場のモータに交流電圧をかけたところ、電流の波が遅れ、次の複素数電流が流れた。

$$\dot{I}_{\text{motor}} = 8 - 6j\text{ A}$$

このモータのすぐ横に、電流の波を進める性質を持つ「コンデンサ」を並列に接続したところ、コンデンサに次の電流が流れた。

$$\dot{I}_C = 4j\text{ A}$$

次の4つの問いに対する計算プログラムを Colab で作成し、答えを弾き出しなさい。

1. コンデンサを繋ぐ前の、大元の電線に流れる**電流計の値（矢印の絶対的な長さ）**
2. コンデンサを繋いだ後の、**合成された全体電流の複素数表示**
3. コンデンサを繋いだ後の、大元の電線に流れる **電流計の値（矢印の絶対的な長さ）**
4. コンデンサを繋いだ結果、大元の電線の電流計の値は **増えたか、減ったか**

### 💡 ヒントコード（SymPy版）

複素数の絶対値（長さ）は `sp.Abs()`、小数への変換は `sp.N()` が使えます。

```python
import sympy as sp

j = sp.I  # SymPyでは大文字のIが虚数単位です

I_motor = 8 - 6 * j
I_C = 4 * j

print(sp.Abs(I_C))        # 複素数の絶対値
print(sp.N(sp.Abs(I_C)))  # (人間にわかりやすい) 小数点にする

# ここに合成電流の計算や、Absを使った長さの計算を追記する
```

---

## 📝 課題B：ベクトルによるクレーンの仕事計算

工場のクレーンで重い荷物を斜めに引っ張った。加えた力ベクトル $\vec{F}$ と、荷物が動いた移動ベクトル $\vec{d}$ は以下の通りである。

$$\vec{F} = \begin{bmatrix} 120 \\ 50 \end{bmatrix}\text{ N},\quad \vec{d} = \begin{bmatrix} 3 \\ 0 \end{bmatrix}\text{ m}$$

次の値を求めるプログラムを Colab で作成しなさい。

1. 力ベクトル $\vec{F}$ の大きさ（ノルム）
2. 移動ベクトル $\vec{d}$ の大きさ（移動距離）
3. クレーンが荷物にした仕事 $W = \vec{F} \cdot \vec{d}$
4. **【考察】** 上向きの 50 N の力成分が、なぜ今回の仕事 $W$ の結果に影響を与えていない（効いていない）のか。その理由を、今回の台車（荷物）が動いた方向と絡めて、Markdownセルに事実に基づき客観的に説明しなさい（1〜2文）。

### 💡 ヒントコード（NumPy版）

```python
import numpy as np

F = np.array([120, 50])
d = np.array([3, 0])

# ここに np.linalg.norm や np.dot を使った計算を追記する
```
### ✍️ Markdownセルへのレポート記述テンプレート（例）

Colabのテキストセルを作って、以下のように論理的に締めくくりましょう。

```text
【考察】
荷物は水平方向にのみ移動しており、垂直（上）方向への移動量は0である。
そのため、垂直方向の力成分である50Nは仕事に寄与しない。
Pythonによる内積の計算結果から、このクレーンがした有効な仕事は〇〇Jであることを確認した。
```

---

## 📝 課題C：AIレコメンドシステムの裏側

ある映画配信サービスでは、ユーザーの「好みの傾向」と、映画の「ジャンルの要素」をそれぞれ **3次元のベクトル** で表し、内積を使って次に提案すべき映画を決定しています。

要素は左から順に **（SF度, 恋愛度, アクション度）** の3つで、それぞれ1〜5点で数値化されています。

* **ユーザーAの好みベクトル** 　　　$\vec{u} = (5, 1, 4)$
*（SFとアクションが大好き、恋愛映画にはほぼ興味がない）*

* **新作映画Xの特徴ベクトル**　　$\vec{v} = (4, 1, 5)$

* **新作映画Yの特徴ベクトル**　　$\vec{w} = (1, 5, 1)$

内積 $\vec{u} \cdot \vec{v}$ と $\vec{u} \cdot \vec{w}$ をそれぞれ計算し、AIとしてユーザーAにどちらの映画を強くおすすめすべきか判定してください。

### 📌 補足説明：2次元から3次元に増えても、やることは同じ！

「次元が増えると難しそう…」と思うかもしれませんが、怯える必要はまったくありません。
2次元のときと **計算のルール（演算）は完全に同じ** です。

* **2次元の場合:** `(1番目 × 1番目) + (2番目 × 2番目)`
* **3次元の場合:** `(1番目 × 1番目) + (2番目 × 2番目) + (3番目 × 3番目)`

このように、次元が増えても「同じ位置にある要素同士を掛け算して、最後に全部足す」というシンプルなルールのままです。仮にこれが100次元になっても、やることは変わりません。

### 🧠 データサイエンスの視点から

内積の本質は「お互いのこだわり（数字が大きい成分）がどれだけ一致しているか」の判定です。

* **映画X:** ユーザーAが好きな「SF（5）」と「アクション（4）」に対して、映画側も高い数値（4と5）を持っていたため、掛け算によってスコアが爆発的に跳ね上がります。
* **映画Y:** ユーザーAが興味のない「恋愛（1）」に全振り（5）しているため、掛け算をしても点数が伸びません。

このように、内積は「人間の好みのマッチング」を計算するのに非常に強力な道具として、現代のAI技術で大活躍しています。
