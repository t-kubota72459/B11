# 第14回・第15回 合同演習課題：行列解析×ばらつきシミュレーション

### 💡 工場のリアルな部品ばらつきを、行列の力でまとめて計算する 💡

---

* **本講義の課題は、Google Colab で作成したノートブック (`.ipynb`) を Google Classroom へ提出します。**
* 以下の内容を1つのノートブックにまとめ、期限までに提出してください。
* **テキストセル**と**コードセル**を使って、学生自身の解答や考察がわかりやすくなるようにレイアウトしてください。
* **提出先:** Google Classroom の指定された課題「第14回・第15回合同課題」にファイルを添付して提出。  
  1. `[ファイル]` ＞ `[ダウンロード]` ＞ `[.ipynb をダウンロード]` を選択。
  2. ダウンロードフォルダにある `工業数学_14_15_合同課題_学籍番号_氏名.ipynb` を提出する。


---

## 🎯 演習のゴール

1. 第14回で学んだ**「回路の連立方程式を行列 $A\vec{x}=\vec{b}$ に落とし込んで解く」**スキルを復習する。
2. 第15回で学んだ**「`np.random.normal()` を使ってカタログ値通りにばらつく部品を生成する」**スキルを組み合わせる。
3. 部品がばらついたときに、完成した製品（回路）ごとに電圧がどれくらい変わってしまうか、PC内で仮想的に組み立てて実験する基礎を掴む。

---

## 📝 演習課題：5つの抵抗を持つ回路の電圧ばらつき評価

第14回で扱った、5つの抵抗 $R_1 \sim R_5$ が複雑に組み合わされた以下の回路をベースにします。

<div align="center">
<img src="images/ChatGPT Image 2026年6月11日 20_32_53.png" width="40%">
</div>


この回路の点Aと点Bの電圧（ $V_A, V_B$ ）を求めるための行列方程式 $A\vec{x}=\vec{b}$ は、以下の通りでした。

$$\begin{bmatrix}
\frac{1}{R_1} + \frac{1}{R_2} + \frac{1}{R_3} & -\frac{1}{R_3} \\
-\frac{1}{R_3} & \frac{1}{R_3} + \frac{1}{R_4} + \frac{1}{R_5}
\end{bmatrix}
\begin{bmatrix}
V_A \\
V_B
\end{bmatrix}
=
\begin{bmatrix}
\frac{12}{R_1} \\
\frac{5}{R_5}
\end{bmatrix}$$

### 🚨 今回の条件（ばらつきの導入）

工場から送られてきた抵抗には、第15回で学んだ「製造ばらつき」があります。カタログ値（平均値）とばらつき（標準偏差）は以下の通りです。

| 抵抗 | カタログ値（平均値） | 製造ばらつき（標準偏差） |
| --- | --- | --- |
| $R_1$ | $100\,\Omega$ | $1\,\Omega$ |
| $R_2$ | $300\,\Omega$ | $3\,\Omega$ |
| $R_3$ | $200\,\Omega$ | $2\,\Omega$ |
| $R_4$ | $400\,\Omega$ | $4\,\Omega$ |
| $R_5$ | $500\,\Omega$ | $5\,\Omega$ |

---

## 💻 取り組むステップ（Google Colab）

### 【ステップ1】ばらつきのある抵抗を1組作り、行列で電圧を解く

まずは、乱数を使って「ばらついた抵抗 $R_1 \sim R_5$」を1組（製品1台分）生成し、それらを使って行列 `A` とベクトル `b` を組み立て、`np.linalg.solve(A, b)` でその個体の $V_A, V_B$ を計算するコードを書きなさい。

**💡 ヒントコード:**

```python
import numpy as np

# 第15回の技：カタログ値と標準偏差から、ばらついた実際の抵抗値を1台分生成
R1 = np.random.normal(100, 1)
R2 = np.random.normal(300, 3)
R3 = np.random.normal(200, 2)
R4 = np.random.normal(400, 4)
R5 = np.random.normal(500, 5)

# 第14回の技：行列Aとベクトルbを組み立てる
A = np.array([
    [1/R1 + 1/R2 + 1/R3, -1/R3],
    [-1/R3, 1/R3 + 1/R4 + 1/R5]
])

b = np.array([
    12/R1,
    5/R5
])

# 連立方程式を一撃で解く
V = np.linalg.solve(A, b)
VA = V[0]
VB = V[1]

print(f"今回使用された実際のR1: {R1:.2f} Ω")
print(f"計算された点Aの電圧 VA: {VA:.4f} V")
print(f"計算された点Bの電圧 VB: {VB:.4f} V")
```

### 【ステップ2】個体ごとの電圧の変動をチェックする（宿題パート）

上記のコードを何度も「再実行（Ctrl + Enter）」してみなさい。

コードを再実行することは、**「工場で新しい部品を引いてきて、別の回路を新しく1個組み立てた」** ことに相当します。  
再実行するたびに抵抗値が変わり、それに伴って製品ごとの電圧 $V_A, V_B$ もわずかに変動（個体差が発生）することを目視で確認しなさい。

### 【ステップ3】Markdownでレポートをまとめる（宿題パート）

Colabのテキストセルを使い、以下の2点についてあなたの言葉で考察を書きなさい。

1. 使う部品（抵抗）がカタログ値から製品ごとにずれると、完成した回路の出力電圧にはどのような影響が出ると言えるか。
2. もしもこの回路を工場で **1万個作成して（組み立てて）**、電圧が特定の範囲（例：$2.0\mathrm{V} \le V_B \le 3.5\mathrm{V}$）に収まる確率（製品の合格率）を調べたい場合、次回以降どのようなプログラムの構造（文法）を使えば自動で 1 万回組み立てを実行できそうか、予想を立てて書きなさい。

---

## 📥 提出について

* **提出物：** 実験コードと考察をまとめた Google Colab の `.ipynb` ファイル
