# 第16回 シミュレーション：数式で解けない「確率」を数で殴る

### 💡 モンテカルロ法で、部品のばらつきが製品に与える影響を予測する 💡

---

## ⏱ 授業時間の目安（100分授業・正味85分）

| 時間 | 内容 |
|---:|---|
| 0〜5分 | 今日のゴール、第15回（統計・品質管理）の復習 |
| 5〜25分 | モンテカルロ法とは？：PCの中で何万回も実験を繰り返す力技 |
| 25 Muffins〜45分 | 💻 ワーク1：ばらつきを持つ「仮想の抵抗」を乱数で1個ずつ生み出す |
| 45〜65分 | 💻 ワーク2：【回路設計】分圧回路の電圧ばらつきシミュレーション（1万回ループ） |
| 65〜85分 | 💻 ワーク3：1万回の実験結果から、システムの「合格率」を弾き出す |

---

## 🎯 今日のゴール

今日の授業の目標は以下の4つです。

1. **モンテカルロ法** を「数式で計算するのが難しい確率の問題を、大量の乱数を使って力技で解く手法」として理解する。
2. `np.random.normal()` と `for` 文を組み合わせ、カタログ通りの「ばらつきを持つ部品」をPC内で1個ずつ組み立てられるようになる。
3. 1万回実験した結果をリストに貯め、NumPy 配列に変換して一括処理するデータハンドリングを学ぶ。
4. 前回学んだ統計処理（平均・標準偏差・ヒストグラム・合格率）をシミュレーション結果に適用し、製品の品質を予測できるようになる。

---

## 1. 【ぶっちゃけトーク】なぜ「シミュレーション」が必要なのか？

製品を設計するとき、使う部品（抵抗、コンデンサ、金属の軸など）には必ず前回の授業でやった「ばらつき」があります。
たとえば、カタログに「 $100\,\Omega$ 」と書かれた抵抗を1000個買ってきても、実際は $99\,\Omega$ だったり $101\,\Omega$ だったりします。

部品が1つだけなら簡単ですが、**「ばらつく部品」が複数組み合わさったとき、最終的な製品の出力がどれくらい狂ってしまうか** を数式（確率密度関数の合成など）で厳密に計算するのは、数学的にめちゃくちゃ難解になります。

「数式で解くのが難しいなら、**PCの中にばらつきのある部品を1万個作って、1万回組み立てて動かしてみればいいじゃないか**」
この、大量のサイコロ（乱数）を振って強引に確率や未来を予測する手法を、カジノの街の名前にちなんで **『モンテカルロ法』** と呼びます。

---

## 2. 💻 ワーク1：ばらつく部品を乱数で生み出す

前回の授業では `np.random.normal(平均, 標準偏差, 1000)` を使って一気に1000個のデータを作りました。
実際のシミュレーションでは「1個ずつ部品を作って、回路に組み込んで計算する」という流れになります。

まずは、`for` 文の中で「ループが回るたびに、毎回違う値でばらつくリアルな抵抗」を1個ずつ生み出すコードを書いてみましょう。

```python
import numpy as np

# カタログ値（平均）：100 Ω
mean_R = 100
# ばらつき（標準偏差）：1 Ω
std_R = 1

print("--- 倉庫から抵抗を1個ずつ5回取り出してみる ---")
for i in range(5):
    # ループが回るたびに、毎回違う「リアルな抵抗値」が1個生成される
    r = np.random.normal(mean_R, std_R)
    print(f"{i+1}個目の抵抗値: {r:.2f} Ω")
```

実行するたびに、表示される数値が変わることを確認してください。これがPCの中のデジタル工場です。

---

## 3. 💻 ワーク2：分圧回路の電圧を1万回シミュレーションする

では、エンジニアリングの実戦です。
以下の、2つの抵抗で電圧を分ける単純な「分圧回路（ぶんあつかいろ）」を考えます。マイコンのセンサー読み取りなどで日常的に使う必須の回路です。

```text
  入力電圧 (Vin = 10V) ─── [ R1 ] ──┬── [ R2 ] ─── 地面 (0V)
                                   │
                                 出力電圧 (Vout)

```

出力される電圧 $V_{out}$ は、次のオームの法則の式で計算できます。

$$V_{out} = V_{in} \times \frac{R_2}{R_1 + R_2}$$

もし、$R_1 = 100\,\Omega$、$R_2 = 100\,\Omega$ （どちらも標準偏差は $1\,\Omega$ の精密抵抗）を使ったとき、出力電圧 $V_{out}$ は理想通りの5.0Vになるでしょうか？ それとも、どれくらいばらついてしまうでしょうか？ 1万回ループを回して実験してみましょう！

```python
import numpy as np

# 固定の条件
Vin = 10.0
N = 10000  # 試行回数（1万回実験する）

# 計算結果を貯めておくための空のリスト（バケツ）を用意
Vout_list = []

# 1万回の仮想実験スタート
for i in range(N):
    # 毎回、ばらつきを持ったR1とR2が工場で生産される
    R1 = np.random.normal(100, 1) # 平均100, 標準偏差1
    R2 = np.random.normal(100, 1) # 平均100, 標準偏差1
    
    # その時の抵抗値で、出力電圧を計算
    vout = Vin * (R2 / (R1 + R2))
    
    # 計算結果をリスト（バケツ）に追加していく
    Vout_list.append(vout)

# リストを、計算が得意な NumPy の配列（array）に変身させる
Vout_array = np.array(Vout_list)

print("シミュレーション完了！")
print("最初の5回分の実験結果:", Vout_array[:5])
```

---

## 4. 💻 ワーク3：実験結果を統計的に評価し、品質を保証する

1 万回分の実験データ `Vout_array` が手に入りました。
ここからは **第15回（前回）の技** をフルに使い、この回路設計の品質を客観的に評価しましょう。

設計上のルール（顧客との約束規格）は、「出力電圧は $4.95\,\mathrm{V}$ 以上 $5.05\,\mathrm{V}$ 以下でなければならない」とします。

```python
import matplotlib.pyplot as plt

# 前回の技1：平均と標準偏差を出す
v_mean = np.mean(Vout_array)
v_std = np.std(Vout_array)

print(f"Voutの平均値  : {v_mean:.3f} V")
print(f"Voutの標準偏差: {v_std:.3f} V (電圧のばらつき幅)")

# 前回の技2：規格の壁を設定して合格率を計算する
lower = 4.95
upper = 5.05
ok_flags = (Vout_array >= lower) & (Vout_array <= upper)
ok_rate = np.sum(ok_flags) / N

print(f"この設計での製品合格率: {ok_rate * 100:.2f} %")

# 前回の技3：ヒストグラムで山の形を可視化
plt.hist(Vout_array, bins=50, color="lightgreen", edgecolor="black")
plt.axvline(lower, color="red", linestyle="--", label="Lower Limit")
plt.axvline(upper, color="red", linestyle="--", label="Upper Limit")
plt.xlabel("Vout [V]")
plt.ylabel("Count")
plt.title("Simulation Result of Vout (Initial Design)")
plt.legend()
plt.show()
```

#### 🎯 実行結果の読み取り

部品（抵抗）のばらつきがわずか $1\,\Omega$（1%）であっても、組み合わさることで最終的な出力電圧 $V_{out}$ に綺麗な山型のばらつきが発生し、赤い点線（規格）の外側に不良品がわずかに発生する様子が完璧に予測できました。

---

## 5. まとめ：これが現場のワークフローだ

1. **現実をモデル化する：** 物理式・回路の式を立てる（ $V_{out} = V_{in} \frac{R_2}{R_1+R_2}$ ）
2. **部品をばらつかせる：** `np.random.normal()` でリアルな不確実性を再現する
3. **数で殴る：** `for` 文で何万回も計算してデータを集める（モンテカルロ法）
4. **品質を評価する：** 前回学んだ平均・標準偏差・合格率・ヒストグラムで結論を出す

★ **次回（第17回・最終回）は、完全にみなさんのレポート作成時間にあてます。**
新しく習うコードや数式は一切ありません。今日書いたコードがそのまま最大の武器になります。

次回は、今回作った回路の「抵抗を半額の安い部品（ばらつき大）に変えたらどうなるか？」という **コストダウンの比較実験** を行ってもらいます。「いくら安くなるか」「品質は維持できるか」をデータで示し、Markdownでエンジニアとしての意見を論理的にまとめる、最終レポート（総合設計演習）に挑戦します！
